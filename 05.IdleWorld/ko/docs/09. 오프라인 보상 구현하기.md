# [📖 심화 학습] 오프라인 보상 구현하기
<br>
<br>
<br>

## 영상 타임라인
- 강의 영상내 아래의 타임라인에서 학습할 수 있습니다.
- 맵 타입 이해하기: `01:28:07`

<br>

## 학습 목표

- 플레이어의 마지막 접속 시간과 현재 시간을 비교하는 기능을 구현합니다.
- 지정한 계산식에 따라 재화를 지급하는 기능을 구현합니다.

<br>

## 오프라인 보상

<p align="center">
<img src="../../_images/MSW-100_Idle_9_001.png" alt="MSW-100_Idle_9_001.png">
</p>

- 오프라인 보상은 플레이어가 월드를 실행하고 있지 않더라도 보상이 축적되는 기능입니다.
- 플레이어가 월드에 재접속하도록 유도하는 장치이면서 플레이어는 보상을 통해 자신의 캐릭터를 강화시킬 수 있습니다.

<br>

### Logic 제작하기
- 오프라인 보상을 구현하기 위해 재화를 관리하는 Logic과 시간에 따른 보상을 지급하는 Logic을 제작했습니다.
- 샘플 월드에서는 **GameLogic**, **TimeRewardLogic**이란 이름으로 제작되었습니다.
- 아래는 **GameLogic**, **TimeRewardLogic**의 코드입니다.

```lua
@Logic
script GameLogic extends Logic

property number summonToken = 0

property boolean needTimer = false

property number maxLeftTime = 10.0

property number leftTimeDelta = 0

@ExecSpace("ClientOnly")
method void NeedResult()
-- 현재 맵이 InGame인지 점검합니다.
if _UserService.LocalPlayer.CurrentMapName == "InGame" then
	-- InGame 상태일 경우 MonsterSpawnController를 찾아 부여합니다.
	local MonsterSpawnController = _EntityService:GetEntityByPath("/maps/InGame/MonsterSpawnController")
	local monsterSpawnComponent = MonsterSpawnController.MonsterSpawnController
	local userId = _UserService.LocalPlayer.PlayerComponent.UserId
	monsterSpawnComponent:ChangeResultUI()
	-- 현재 웨이브와 최고 웨이브를 변수에 저장합니다.
	local currentWave = monsterSpawnComponent.waveIndex
	local bestWave = _DataStorageLogic.clientBestWave
	-- 이번 웨이브의 달성도가 기존 Wave보다 클 경우에만 이를 저장합니다.
	if currentWave > bestWave then
		_DataStorageLogic:RequestSaveBestWave(userId, currentWave)
		-- 갱신 직후 클라이언트 값도 즉시 최신화해줍니다.
		_DataStorageLogic.clientBestWave = currentWave
		bestWave = currentWave
	end
	-- 달성한 Wave와 저장된 Wave를 비교하여 UI에 출력되도록 넘겨줍니다.
	_UIManageLogic:ShowResultUI(currentWave, bestWave)
end
end

@ExecSpace("ClientOnly")
method void RestartGame()
-- 현재 맵 상태가 InGame인지 점검합니다.
if _UserService.LocalPlayer.CurrentMapName == "InGame" then
	-- MonsterSpawnController를 경로를 기준으로 찾아 등록합니다.
	local MonsterSpawnController = _EntityService:GetEntityByPath("/maps/InGame/MonsterSpawnController")
	local monsterSpawnComponent = MonsterSpawnController.MonsterSpawnController
	-- 게임 재시작시 MonsterSpawnController가 수행해야 하는 기능을 요청합니다.
	monsterSpawnComponent:CallRestart()
    -- characterSlotPosGroup을 찾아 등록합니다.
	local characterSlotController = _EntityService:GetEntityByPath("/maps/InGame/CharacterSlotPosGroup")
	local characterSlotGroupComponent = characterSlotController.CharacterSlotGroupComponent
	-- characetrSlotGroupComponent의 자식 엔티티인 캐릭터들을 모두 지웁니다.
	characterSlotGroupComponent:ClearCharacterList()
    -- 만약 현재 summonTokken이 0 이하일 경우 게임의 플레이는 가능하도록 만들기 위해 summonTokken을 1로 변경합니다.
	if self.summonToken <= 0 then
		self:UpdateSummonTokken(1)
	end
	self:DisableTimer()
end
end

@ExecSpace("ClientOnly")
method void OnBeginPlay()
-- 오프라인 보상을 요청하고 이를 적용합니다.
self:CallOfflineReward()
-- 로컬 플레이어의 ID값을 가져온 뒤 해당 유저의 최고 기록을 미리 가져옵니다.
local userId = _UserService.LocalPlayer.PlayerComponent.ProfileCode
_DataStorageLogic:RequestLoadBestWave(userId)
end

@ExecSpace("ClientOnly")
method void CallOfflineReward()
-- offlineReward를 요청한 뒤 이를 적용합니다.
local getUserID = _UserService.LocalPlayer.PlayerComponent.ProfileCode
self:RequestOfflineReward(getUserID)
end

@ExecSpace("Server")
method void RequestOfflineReward(string UserID)
-- 마지막으로 접속했던 시간을 요청합니다.
_TimeRewardLogic:LoadLastLoginTime(UserID)
end

@ExecSpace("ClientOnly")
method void UpdateSummonTokken(number ChangeValue)
-- 전달받은 값으로 SummonTokken을 갱신한 뒤 UI를 갱신합니다.
self.summonToken = self.summonToken + ChangeValue
_UIManageLogic:UpdateSummonTokken(self.summonToken)
end

@ExecSpace("ClientOnly")
method void NeedTimer()
-- 타이머가 필요한 상태로 전환하여 타이머가 작동하도록 요청합니다.
self.needTimer = true
self.leftTimeDelta = self.maxLeftTime
end

@ExecSpace("ClientOnly")
method void DisableTimer()
-- 웨이브의 적을 모두 처치하여 타이머가 필요 없는 상태로 변경합니다.
self.needTimer = false
_UIManageLogic:DisableTimer()
end

@ExecSpace("ClientOnly")
method void OnUpdate(number delta)
-- 만약 needTimer가 true일 경우 Timer를 시작합니다.
if self.needTimer then
	-- 만약 leftTimeDelta가 0 이하일 경우 게임오버 처리를 시작합니다.
	if self.leftTimeDelta <= 0 then
		self.needTimer = false
		self.leftTimeDelta = 0
		_UIManageLogic:UpdateTimer(self.leftTimeDelta)
		self:NeedResult()
	else
		-- 아직 남은 시간이 존재할 경우 시간 감소 및 UI 처리를 수행합니다.
		self.leftTimeDelta = self.leftTimeDelta - delta
		if self.leftTimeDelta <= 0 then
			self.leftTimeDelta = 0
			end
			_UIManageLogic:UpdateTimer(self.leftTimeDelta)
	end	
end
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

```lua
@Logic
script TimeRewardLogic extends Logic

property string storageKey = "LastLoginTime"

@Sync
property number offlineReward = 0

@Sync
property boolean offlineRewardComplete = false

@ExecSpace("ServerOnly")
method void SaveCurrentTime(string userId)
    local currentTime = os.time()
    local userDataStorage = _DataStorageService:GetUserDataStorage(userId)
    
    -- "LastLoginTime"이라는 키로 현재 os.time() 값을 저장합니다.
    userDataStorage:SetAsync(self.storageKey, tostring(currentTime), function(errorCode, key)
        if errorCode == 0 then
            log(userId .. "의 접속 시간이 성공적으로 저장되었습니다: " .. currentTime)
        else
            log("저장 실패! 에러 코드: " .. errorCode)
        end
    end)
end

@ExecSpace("ServerOnly")
method void LoadLastLoginTime(string userId)
local userDataStorage = _DataStorageService:GetUserDataStorage(userId)
    
userDataStorage:GetAsync(self.storageKey, function(errorCode, key, value)
if errorCode == 0 then
	-- 이전에 저장된 시간이 없다면 value는 nil일 수 있습니다.
	local lastTime = tonumber(value) or 0
            
	-- 앞서 만든 계산 로직을 호출하여 보상 수치를 구합니다.
	self.offlineReward = self:CalculateTimeReward(lastTime)
	-- 보상 수령을 완료했으니 현재 시간을 저장합니다.
	self:SaveCurrentTime(userId)
	end
end)
end

@ExecSpace("ServerOnly")
method number CalculateTimeReward(number lastLoginTime)
-- 마지막 접속 시간과 현재 시간을 비교하여 필요한 보상을 계산하여 지급합니다.
self.offlineRewardComplete = true
if lastLoginTime == nil or lastLoginTime == 0 then
	return 1
end

local currentTime = os.time()
local timeDiff = currentTime - lastLoginTime

if timeDiff < 0 then 
	timeDiff = 0 
end
	
if timeDiff <= 1 then
	return 1
else
	-- 10초마다 SummonTokken을 1씩 추가로 지급합니다.
	local returnValue = math.floor(timeDiff / 10)
	-- 최대 10개까지만 지급하기 위한 처리를 진행합니다.
	if returnValue >= 10 then
		returnValue = 10
	end
    return returnValue
end
   
end

@ExecSpace("ClientOnly")
method void OnSyncProperty(string name, any value)
-- offlineReward 값이 변경되었을 경우 SummonTokken을 갱신합니다.
if name == "offlineReward" then
	_GameLogic:UpdateSummonTokken(value)
end
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

- 샘플 월드에서는 미접속 시간의 차이가 10초일 때 소환 토큰 1개씩을 지급하며, 최대 10개까지 지급합니다.
- **TimeRewardLogic**이 계산한 결과값을 **GameLogic**의 summonTokken에 대입합니다.
- 플레이어는 summonTokken의 여부에 따라 캐릭터를 소환할 수 있습니다.

<br>

## ⚠️ 오프라인 보상 구현시 주의사항
- **보안 및 어뷰징**
    - 사용자가 디바이스의 시간을 설정하여 시간을 미래로 설정할 경우 그만큼의 시간이 흐른 것으로 간주하여 대량의 보상을 지급할 수 있습니다.
- **게임 경제 및 밸런스**
    - 오프라인 보상을 구현했으나 상한치인 캡(Max Cap)을 설정하지 않을 경우 게임의 경제가 크게 무너질 수 있습니다.
- **비율 및 소수점 처리**
    - 1분당 10골드를 지급한다고 가정 할 경우 30초만 지나고 접속했을 경우 기준을 어떻게 설정할 것 인지 문제가 발생합니다.
- **타임존 문제**
    - 이용자가 해외여행을 할 경우 서버에 저장된 시간대와 달라 보상을 더 크게 지급하거나 못받을 수 있습니다.
- **유저 경험 및 예외 처리**
    - 만약 인벤토리의 개수 제한이 있는 게임일 경우 오프라인 보상이 슬롯보다 많을 경우 보상의 유실이 발생할 수 있습니다.
    - 보상을 수령할 때 강제 종료 또는 네트워크 끊김으로 인해 보상 지급 도중 데이터가 날아갈 수 있음

### 해결 방안
- 위의 문제들을 해결하기 위한 방안으로 다음과 같은 방법들이 있습니다.
- **보안 및 어뷰징**
    - 값의 처리를 서버에서만 연산하도록 만들어 클라이언트가 개입할 상황을 막는 방법이 있습니다.
- **게임 경제 및 밸런스**
    - 보상의 상한선인 캡(Max Cap)을 만들어 사용한다면 보상이 과도하게 지급되는걸 막을 수 있습니다.
- **비율 및 소수점 처리**
    - 이는 기획적 문제로 규칙을 설정한뒤 해당 규칙에 예외가 발생하지 않도록 조절해야 합니다.
- **타임존 문제**
    - 국제 표준시간(UTC)을 활용하여 이러한 문제를 예방할 수 있습니다.
- **유저 경험 및 예외처리**
    - 보상이 지급되기 전 충분한 여유 공간이 있는지 예외처리를 수행합니다.
    - 네트워크 통신이 필요한 기능일 경우 네트워크 통신에 실패했는지 확인하고 실패했다면 2~3회 반복 요청하여 통신 오류를 줄일 수 있습니다.


<br>

## 해당 영상 시청 후 수행해 볼 내용

- 플레이어에게 접속 시간의 차이에 따른 보상을 지급할 수 있도록 보상 재화를 설계 및 제작합니다.
- 시간에 따라 일정한 비율로 보상을 제공할 수 있도록 로직을 구성, 제작합니다.

<br>

## 최소 구현 기준
- 접속 시간의 편차에 따라 플레이어에게 소환 재화를 지급합니다.
- 플레이어에게 재화가 정상적으로 지급되며, 지급된 시점에 접속 시간을 저장합니다.

<br>

## 응용 및 확장 방향
- 플레이어가 사용 가능한 재화의 종류를 확장하고 오프라인 보상을 통해 획득할 수 있도록 기능을 추가합니다.
- 플레이어의 재화를 저장하고 불러올 수 있도록 제작하여 플레이어가 지속적으로 월드에 접근하도록 콘텐츠를 추가합니다.
- 오프라인 보상을 데이터 테이블을 활용하여 다양한 재화의 지급량을 조절할 수 있도록 구조를 변경합니다.