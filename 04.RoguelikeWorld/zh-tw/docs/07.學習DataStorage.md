# [📖 進階學習] 學習DataStorage
<br>
<br>
<br>

## 影片時間軸
- 可在課程影片中以下時間點學習。
- 理解地圖類型：`02:03:14`

<br>

## 學習目標
- 學習玩家達成最高紀錄時的儲存方法。
- 學習讀取已儲存資訊的方法。

<br>

## 觀看該影片後可嘗試執行的內容
- 製作將玩家最高紀錄輸出至UI的功能。

---

<br>

## 儲存與讀取系統
<p align="center">
<img src="../../_images/MSW-100_Survivorslike_7_001.png" alt="MSW-100_Survivorslike_7_001.png">
</p>

- 玩家會懷念自己最強的瞬間。
- 若將這些瞬間以視覺資訊儲存，玩家便會為了保留更好的狀態而反覆遊玩遊戲。
- 這次將學習如何儲存與讀取玩家最高紀錄。

<br>

### 製作DataStorageLogic
```lua
@Logic
script DataStorageLogic extends Logic

@Sync
property number bestKillScore = 0

property number clientBestKillScore = 0

@ExecSpace("Client")
method void RequestBestKillScore()
-- 取得本地玩家ID值。
local localPlayerID = _UserService.LocalPlayer.PlayerComponent.UserId
-- 將回傳值設定為0。
local getValue = ""
self:GetPlayerBestScore(localPlayerID)
end

@ExecSpace("Server")
method void GetPlayerBestScore(string UserID)
-- 使用傳入的UserID取得DataStorage。
local userDataStorage = _DataStorageService:GetUserDataStorage(UserID)
-- 若結果值為nil，則將return值設定為0。
if userDataStorage == nil then
	self.bestKillScore = 0
end
-- 若正常找到資料，則取得值。
local errorCode, bestScore = userDataStorage:GetAndWait("BestScore")
-- 若errorCode為0，代表正常結果，因此更新bestKillScore。
if errorCode == 0 then
	self.bestKillScore = tonumber(bestScore)
else
	self.bestKillScore = 0
end
end

@ExecSpace("Client")
method void RequestSaveBestKillScore(number KillScore)
-- 取得本地玩家ID值。
local localPlayerID = _UserService.LocalPlayer.PlayerComponent.UserId
-- 更新clientBestKillScore。
self.clientBestKillScore = KillScore
-- 依照傳入的KillScore嘗試儲存。
self:SaveBestKillScore(KillScore, localPlayerID)
end

@ExecSpace("Server")
method void SaveBestKillScore(number KillScore, string LocalPlayerID)
self.bestKillScore = KillScore
-- 使用傳入的UserID取得DataStorage。
local userDataStorage = _DataStorageService:GetUserDataStorage(LocalPlayerID)
-- 嘗試進行儲存。
userDataStorage:SetAndWait("BestScore", tostring(self.bestKillScore))
end

@ExecSpace("ClientOnly")
method void OnSyncProperty(string name, any value)
if name == "bestKillScore" then
	if value ~= nil then
		self.clientBestKillScore = value
		_UIManageLogic:UpdateTitleBestScore()
	else
		self.clientBestKillScore = 0
		_UIManageLogic:UpdateTitleBestScoreZero()
	end
end
end

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:RequestBestKillScore()
end

end
```

> 該程式碼是以Plain Text為基準編寫而成。

- 範例世界中由名為**DataStorageLogic**的Logic負責執行儲存與讀取。
- 若與最高分數一起呼叫**RequestSaveBestKillScore**Method，則**DataStorageLogic**會儲存該值。
- 相反地，若呼叫**RequestBestKillScore**Method，則會執行讀取已儲存最高分數的功能。

<br>

## 與範例世界同步
- 目前範例世界是以Client為基礎運作。
- 因此不存在由Server更新的Property。
- 為了解決這個問題，不是採用由Client接收並使用Server運算結果的方式，而是採用**Client**傳遞數值後，由**Server**信任並儲存該值的形式運作。

<br>

## 資料竄改

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_7_002.png" alt="MSW-100_Survivorslike_7_002.png">
</p>

- 範例世界的方式存在容易遭資料竄改的問題。
- 為了防止這點，楓之谷世界提供了Server與Client之間的內建同步功能。
- 若要理解該方式，必須理解Server與Client，以及同步的概念。
    - [MSW Documents,伺服器與客戶端](https://maplestoryworlds-creators.nexon.com/zh-tw/docs?postId=207)

---

<br>

## 最低製作標準
- 玩家分數必須能正常儲存與讀取。

<br>

## 應用與進階應用
- 最高擊殺數必須由Server進行計算與處理。
- Client必須以更新由Server處理完成的結果值的方式建構功能。
- 製作讓玩家儲存目前狀態後，可以接續遊玩的功能。