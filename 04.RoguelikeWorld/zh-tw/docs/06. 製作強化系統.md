# [📖 進階學習] 製作強化系統
<br>
<br>
<br>

## 影片時間軸
- 可在課程影片中以下時間點學習。
- 製作強化系統：`01:47:59`

<br>

## 學習目標
- 學習製作玩家升級時進行強化之功能的方法。
- 建立並使用管理強化系統資料的方法。

<br>

## 觀看該影片後可嘗試執行的內容
- 製作當**PlayerStatController**的AddEXPValue Method執行時，若玩家經驗值足夠，便顯示強化UI的功能。
- 建構功能，使效果能依照選擇的強化套用。

---

## 強化玩家

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_001.png" alt="MSW-100_Survivorslike_6_001.png">
</p>

- 若玩家升級，便依照升級建構功能，讓玩家變強。
- 範例世界中，作為變強的手段，是以提高能力值數值的形式建構功能。

<br>

### 製作強化UI

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_002.png" alt="MSW-100_Survivorslike_6_002.png">
</p>

- 查看範例世界中的**LevelUpUI**時，可見其由下列組成。
    - **LayoutGroup**：與製作**SelectCharacterUI**時使用的**LayoutGroup**執行相同作用。
        - 負責將子實體Slot1、Slot2、Slot3依照一定間距排列。
    - **Slot**：總共存在3個實體，各Slot會提供不同強化項目。
- 為了製作強化UI，活用前面學過的方法製作**LevelUpUI**UI Group。
- 接著在**LevelUpUI**新增**LevelUpUIController**。
<br>

#### 製作Slot實體

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_003.png" alt="MSW-100_Survivorslike_6_003.png">
</p>

- Slot實體具有下列結構。
    - **Slot**：負責強化Slot的實體。
        - 持有用來輸出強化Slot背景的**SpriteGUIRendererComponent**。
        - 持有**ButtonComponent**以執行按鈕功能。
        - 持有**LevelUpSlotController**，用來依照Slot資料更新UI。
    - **IconImage**：持有**SpriteGUIRendererComponent**，用來輸出強化圖片。
    - **InfoTextFrame**：持有**SpriteGUIRendererComponent**，用來輸出顯示強化說明的**InfoText**框架。
    - **InfoText**：持有**TextGUIRendererComponent**，用來說明該Slot的強化效果。

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_004.png" alt="MSW-100_Survivorslike_6_004.png">
</p>

- 查看**IconImage**與**InfoTextFrame**的**SpriteGUIRendererComponent**時，會看到名為**RaycastTarget**的Property。
- 必須解除該值。
    - 若未解除，圖示圖片與說明視窗框架可能會忽略玩家的觸碰、點擊。
    - 這可能會導致玩家的選擇畫面操作空間狹窄的問題。
- 若**Slot**製作完成，便複製**Slot**製作成3格。

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_005.png" alt="MSW-100_Survivorslike_6_005.png">
</p>

- 若3個**Slot**複製完成，點擊**LayoutGroup**實體後，變更**ScrollLayoutGroupComponent**的值。
    - **Type**：為了水平排列，變更為**Horizontal**。
    - **Spacing**：依照製作的UI自由變更數值，讓**Slot**之間留有間距。範例世界中設定為50。
    - **ChildAlignment**：將**Slot**的排列起點變更為垂直置中，水平從左側開始的**MiddleLeft**。
    - **UseScroll**：預計只使用3個欄位，因此解除**UseScroll**的值。

<br>

### 製作EnhanceData DataSet
- 製作用來管理強化相關資訊的DataSet。
- 範例世界中製作並使用**EnhanceData**DataSet。

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_006.png" alt="MSW-100_Survivorslike_6_006.png">
</p>

<style type="text/css">
.tg  {border-collapse:collapse;border-spacing:0;}
.tg td{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  overflow:hidden;padding:10px 5px;word-break:normal;}
.tg th{border-color:black;border-style:solid;border-width:1px;font-family:Arial, sans-serif;font-size:14px;
  font-weight:normal;overflow:hidden;padding:10px 5px;word-break:normal;}
.tg .tg-cly1{border-color:currentcolor;text-align:left;vertical-align:middle}
.tg .tg-xwyw{border-color:currentcolor;text-align:center;vertical-align:middle}
.tg .tg-0a7q{border-color:currentcolor;text-align:left;vertical-align:middle}
</style>
<table class="tg"><thead>
  <tr>
    <th class="tg-xwyw">名稱</th>
    <th class="tg-xwyw">說明</th>
  </tr></thead>
<tbody>
  <tr>
    <td class="tg-0a7q">TargetPropertyName</td>
    <td class="tg-0a7q">用來指定PlayerStatController Property的值。</td>
  </tr>
  <tr>
    <td class="tg-0a7q">NumberValue</td>
    <td class="tg-0a7q">用來指定TargetPropertyName要提升多少的數值。</td>
  </tr>
  <tr>
    <td class="tg-0a7q">IconID</td>
    <td class="tg-0a7q">用來登錄輸出欄位圖片的SpriteRUID值。</td>
  </tr>
  <tr>
    <td class="tg-cly1">DescriptionID</td>
    <td class="tg-cly1">用來登錄UIStringData Key值的值。</td>
  </tr>
</tbody>
</table>

<p align="center">
<img src="../../_images/MSW-100_Survivorslike_6_007.png" alt="MSW-100_Survivorslike_6_007.png">
</p>

- 可以看到與**PlayerStatController**Property名稱相同的項目已登錄為**EnhanceData**的**TargetPropertyName**。
- 這是為了統一兩個項目的名稱，讓DataSet能快速新增強化項目。
    - ⚠️ 因此**PlayerStatController**的Property名稱與**EnhanceData**的**TargetPropertyName**不可不同。
- **EnhanceData**DataSet製作完成後，製作**EnhanceDataType**StructType。

<br>

### 查看LevelUpLogic

```lua
@Logic
script LevelUpLogic extends Logic

property table levelUpData = {}

property table enhanceData = {}

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitLevelUpData()
self:InitEnhanceData()
end

@ExecSpace("ClientOnly")
method void InitLevelUpData()
-- 檢查levelUpData是否為nil。
if not isvalid(self.levelUpData) or not next(self.levelUpData) then
	-- 若為nil，則尋找DataSet。
	local dataSet = _DataService:GetTable("LevelUpData")
	-- 取得找到的DataSet RowCount。
	local rowCount = dataSet:GetRowCount()
	-- 依照rowCount重複輸入資料。
	for i = 1, rowCount do
		local levelData = LevelUpDataType()
		levelData.Level = tonumber(dataSet:GetCell(i, "Level"))
		levelData.NeedEXP = tonumber(dataSet:GetCell(i, "NeedEXP"))
		self.levelUpData[i] = levelData
	end
end
end

@ExecSpace("ClientOnly")
method LevelUpDataType GetLevelData(number CurrentLevel)
-- 檢查levelUpData中是否存在對應等級值。
if isvalid(self.levelUpData[CurrentLevel]) then
	-- 若值正常存在，則回傳該值。
	return self.levelUpData[CurrentLevel]
end
end

@ExecSpace("ClientOnly")
method number GetMaxLevel()
-- 回傳levelUpData的總數量。
return #self.levelUpData
end

@ExecSpace("ClientOnly")
method void InitEnhanceData()
-- 檢查enhanceData是否為nil。
if not isvalid(self.enhanceData) or not next(self.enhanceData) then
	-- 若enhanceData為nil，則取得DataSet。
	local findData = _DataService:GetTable("EnhanceData")
	-- 取得findData的RowCount。
	local rowCount = findData:GetRowCount()
	-- 依照rowCount重複取得資料後進行登錄。
	for i = 1, rowCount do
		local data = EnhanceDataType()
		data.TargetPropertyName = tostring(findData:GetCell(i, "TargetPropertyName"))
		data.IconID = tostring(findData:GetCell(i, "IconID"))
		data.NumberValue = tonumber(findData:GetCell(i, "NumberValue"))
		data.DescriptionID = tostring(findData:GetCell(i, "DescriptionID"))
		self.enhanceData[i] = data
	end
end
end

@ExecSpace("ClientOnly")
method table GetRandomEnhanceTable()
-- 建立用來回傳的dataList。
local dataList = {}
-- 宣告用來在迴圈中指定順序的count。
local count = 1
-- 在條件不滿足前持續執行迴圈。
while count <= 3 do
	-- 從enhanceData數量中取得隨機值。
	local getNumber = _UtilLogic:RandomIntegerRange(1, #self.enhanceData)
	-- 將指定值登錄至data。
	local data = self.enhanceData[getNumber]
		local isDuplicate = false
	-- 若dataList為空狀態，則登錄首次選中的值。
	-- 若已存在值，則重複for文尋找是否有相同值。
	for i = 1, #dataList do
        if dataList[i] == data then
            isDuplicate = true
            break
        end
    end
	if not isDuplicate then
        dataList[count] = data
        count = count + 1
    end
end

return dataList
end

end
```

> 該程式碼是以Plain Text為基準編寫而成。

- 查看前面製作的**LevelUpLogic**時，可以看到**GetRandomEnhanceTable**Method。
- 該Method會傳遞隨機3個強化選項，並組成由之後製作的**LevelUpUiController**將傳入值更新至各Slot的形式。

<br>

### 製作Component
- 首先為了建立傳入的Data，撰寫**LevelUpSlotController**。

```lua
@Component
script LevelUpSlotController extends Component

property SpriteGUIRendererComponent iconRenderer = nil

property TextGUIRendererComponent infoTextRenderer = nil

property EnhanceDataType enhanceData = EnhanceDataType()

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitDefault()
end

method void InitDefault()
-- 檢查iconRenderer是否為nil。
if not isvalid(self.iconRenderer) then
	-- 若iconRenderer為nil，則尋找後登錄。
	self.iconRenderer = self.Entity:GetChildByName("IconImage", false).SpriteGUIRendererComponent
end
-- 檢查infoTextRenderer是否為nil。
if not isvalid(self.infoTextRenderer) then
	-- 若infoTextRenderer為nil，則尋找後登錄。
	self.infoTextRenderer = self.Entity:GetChildByName("InfoText", true).TextGUIRendererComponent
end
-- 初始化enhanceData。
self.enhanceData = EnhanceDataType()
end

@ExecSpace("ClientOnly")
method void InitSlotData(EnhanceDataType Data)
-- 將傳入的Data登錄至enhanceDataType。
self.enhanceData = Data
-- 使用傳入的Data更新圖示圖片與說明。
self.iconRenderer.ImageRUID = self.enhanceData.IconID
self.infoTextRenderer.Text = _LocalizationService:GetTextFormat(self.enhanceData.DescriptionID, self.enhanceData.NumberValue)
end

@EventSender("Self")
handler HandleButtonClickEvent(ButtonClickEvent event)
-- 將自身資訊傳遞給GameLogic，請求玩家強化。
_GameLogic:RequestEnhancePlayer(self.enhanceData)
end

end
```

> 該程式碼是以Plain Text為基準編寫而成。

- 持有Slot的**LevelUpUIController**會透過各欄位的**LevelUpSlotController**的**InitSlotData**，請求更新欄位UI。
- 接著製作**LevelUpUIController**。

```lua
@Component
script LevelUpUIController extends Component

property table slotTable = {}

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitSlot()
end

@ExecSpace("ClientOnly")
method void InitSlot()
-- 檢查SlotTable是否為nil狀態。
if not isvalid(self.slotTable) or not next(self.slotTable) then
	-- 若為nil，則嘗試初始化。
	for i = 1, 3 do
		-- 設定想要尋找的實體名稱。
		local targetEntityName = tostring("Slot"..i)
		-- 以名稱尋找自身子實體。
		local entity = self.Entity:GetChildByName(targetEntityName, true)
		self.slotTable[i] = entity.LevelUpSlotController
	end
end
end

@ExecSpace("ClientOnly")
method void ShowLevelUpUI()
self.Entity.Enable = true
self:SetLevelUpUI()
end

@ExecSpace("ClientOnly")
method void HideLevelUpUI()
self.Entity.Enable = false
end

@ExecSpace("ClientOnly")
method void SetLevelUpUI()
-- 從LevelUpLogic取得隨機強化項目。
local getDataList = _LevelUpLogic:GetRandomEnhanceTable()
for i = 1, #self.slotTable do
	-- 嘗試依照各Data初始化各欄位。
	self.slotTable[i]:InitSlotData(getDataList[i])
end
end

end
```

- 當呼叫**ShowLevelUpUI**時，**LevelUpUIController**會啟用自身。
- 接著透過**SetLevelUpUI**向**LevelUpLogic**呼叫**GetRandomEnhanceTable**，請求取得隨機強化資料。
- 之後便以將傳入的強化資料套用至各Slot的方式更新UI。

<br>

## 製作GameLogic
- 為了將玩家選擇的強化選項傳遞給玩家角色，會使用GameLogic。
- GameLogic在遊戲進行期間，扮演傳遞必要資訊的橋樑角色。
- 查看**LevelUpSlotController**Component的**HandleButtonClickEvent**時，可以看到呼叫GameLogic的部分。
- 範例世界的GameLogic如下製作。

```lua
@Logic
script GameLogic extends Logic

property boolean isPlayingGame = false

property number killCount = 0

@ExecSpace("Client")
method void RequestStartGame(string CharacterID)
-- 檢查傳入的CharacterID是否為正常值。
if _UtilLogic:IsNilorEmptyString(CharacterID) then
	-- 若CharacterID值為空或nil，則輸出錯誤日誌並中斷程式執行。
	log_error("傳入的CharacterID值為nil或空值！")
	return
end

-- 若正常取得CharacterID值，則取得資料。
local getData = _CharacterDataLogic:GetCharacterDataByCharacterID(CharacterID)
-- 使用取得的值初始化玩家角色資訊。
local localPlayer = _UserService.LocalPlayer
localPlayer.PlayerStatController:SetPlayerStat(getData)
-- 請求將UI變更為InGame UI。
_UIManageLogic:ShowInGameUI()
-- 將目前遊戲狀態轉換為true。
self.isPlayingGame = true
-- 將killCount初始化為0。
self.killCount = 0
-- 將LocalPlayer從Title Map移動至InGame。
self:RequestTeleportPlayer(localPlayer, "/maps/InGame/SpawnLocation")
end

@ExecSpace("Server")
method void RequestTeleportPlayer(Entity LocalPlayer, string TeleportMapPath)
_TeleportService:TeleportToEntityPath(LocalPlayer, TeleportMapPath)
end

@ExecSpace("ClientOnly")
method void RequestEnhancePlayer(EnhanceDataType EnhanceData)
-- 尋找本機玩家。
local localPlayer = _UserService.LocalPlayer
-- 尋找本地玩家的PlayerStatController。
local statComponent = localPlayer.PlayerStatController
-- 向PlayerStateController傳遞強化用數值。
statComponent:EnhancePlayer(EnhanceData)
-- 隱藏LevelUpUI。
_UIManageLogic:HideLevelUpUI()
-- 將isPlayingGame狀態轉換為true。
self.isPlayingGame = true
end

@ExecSpace("ClientOnly")
method void NeedEnhancePlayer()
-- 若收到等級提升UI請求，則將isPlayingGame狀態轉換為false。
self.isPlayingGame = false
-- 呼叫UIManageLogic開啟LevelUpUI。
_UIManageLogic:ShowLevelUpUI()
end

@ExecSpace("ClientOnly")
method void CountKillScore()
self.killCount = self.killCount + 1
end

@ExecSpace("Client")
method void CallResult()
-- 將isPlayingGame轉換為false。
self.isPlayingGame = false
-- 與DataStorageLogic的bestKillScore進行比較。
if self.killCount > _DataStorageLogic.clientBestKillScore then
	_DataStorageLogic:RequestSaveBestKillScore(self.killCount)
end
-- 輸出結果畫面。
_UIManageLogic:ShowGameOverUI()

end

@ExecSpace("Client")
method void RequestGoToTitle()
local localPlayer = _UserService.LocalPlayer
-- 請求將UI變更為標題UI。
_UIManageLogic:ShowTitleUI()
-- 將目前遊戲狀態轉換為false。
self.isPlayingGame = false
-- 將LocalPlayer從Title Map移動至Title。
self:RequestTeleportPlayer(localPlayer, "/maps/Title")
end

end
```

> 該程式碼是以Plain Text為基準編寫而成。

- **LevelUpSlotController**透過**GameLogic**的**RequestEnhancePlayer**Method，將選擇了哪種強化傳遞給**PlayerStatController**。

---

<br>

## 最低製作標準

- 隨機強化資料必須正常登錄至UI。
- 玩家選擇的強化項目必須套用至**PlayerStatController**Component。

<br>

## 應用與進階應用

- 除了能力值外，也新增武器強化項目。
- 依照武器強化階段新增投射體數量增加、攻擊範圍增加等各種選項。
- 對強化設定次數限制，防止玩家只投資特定能力值的現象。