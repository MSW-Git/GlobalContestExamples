# [💡기초 학습]월드 환경 구성하기
<br>
<br>
<br>

## 영상 타입라인
- 강의 영상 내 아래의 타임라인에서 학습할 수 있습니다.
- 월드 환경 구성하기: 00:00:00

<br>

## 학습 목표
- 메이플스토리 월드의 가장 기본적인 맵 방식인 Maple TileMap 모드에 대해 학습합니다.
- TileMap 모드에서 타일 맵을 배치하여 게임 환경을 구성할 수 있습니다.
- 디펜스 장르의 특성을 분석하고, 게임 제작 환경을 스스로 구성할 수 있습니다.
- 월드의 맵 내에 다양한 엔티티를 배치할 수 있습니다.

<br>

## 해당 영상 시청 후 수행해 볼 내용
- 기지 외 배경 요소의 엔티티를 제작해 배치해 봅니다.
- 기지 엔티티의 TransformComponent의 속성값을 제어해 더욱 크거나, 작은 기지를 구현합니다.

<br>

## 메이플스토리 월드 맵 모드
<p align="Center">
<img src="../../_images/MSW-100_Defense_1_001.png" alt="MSW-100_Defense_1_001"
</p>

<br>

### TileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_002.png" alt="MSW-100_Defense_1_002"
</p>

- 메이플스토리 월드에서 처음으로 월드를 생성하면 기본 지정되는 맵 형식입니다.
- 메이플스토리의 느낌을 가장 잘 살릴 수 있는 방식의 월드입니다.
- 메이플스토리 월드가 제공하는 Tile을 활용하여 발판을 생성하고, 엔티티를 배치하여 맵을 형성할 수 있습니다.
- 타일의 배치에 따라 경사로를 제작할 수 있습니다.
- 횡스크롤 형태의 게임을 제작할 때, 메이플스토리의 느낌을 살리고 싶을 때 유용한 맵 방식입니다.

<br>

### RectTileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_003.png" alt="MSW-100_Defense_1_003"
</p>

- 탑다운 뷰 형태의 게임을 제작할 때 효과적인 방식의 맵 형식입니다.
- 직접 타일셋을 제작하거나, 메이플스토리 월드에서 제공하는 타일셋 리소스를 활용하여 맵을 형성할 수 있습니다.
- 바둑판 형태로 타일을 배치하여 맵을 형성할 수 있습니다.
- 타일마다 이동 가능과 이동 불가능한 성질을 부여할 수 있습니다.
- TileMap, SideViewRectTileMap 방식과 다르게 4방향, 8방향으로 이동하는 형태의 게임을 제작할 수 있습니다.

<br>

### SideviewRectTileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_004.png" alt="MSW-100_Defense_1_004"
</p>

- 횡스크롤 방식의 게임을 제작할 때 효과적인 방식의 맵 형식입니다.
- 직접 타일셋을 제작하거나, 메이플스토리 월드에서 제공하는 타일셋 리소스를 활용하여 맵을 형성할 수 있습니다.
- 바둑판 형태로 타일을 배치하여 맵을 형성할 수 있습니다.
- RectTileMap과는 다르게 이동 불가능한 타일셋이 아닐 경우, 캐릭터가 타일을 **통과**합니다.
- 따라서 SideViewRectTileMap으로 맵을 형성할 경우, 이동 불가능한 타일셋을 **발판으로** 제작해야 합니다.

<br>

## 지형 배치하기

### TileMap 모드 선택하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_005.png" alt="MSW-100_Defense_1_005"
</p>

- 에디터 상단의 '타일 에디터 버튼'을 길게 누를 경우 맵 모드를 선택할 수 있는 메뉴 목록이 표시됩니다.
- 목록 내 표시되는 'MapleTile'맵 모드를 선택합니다.

ℹ️ 새로운 월드를 제작 후 아무런 환경을 설정하지 않았을 경우 기본 맵 모드는 'MapleTile'로 설정되어 있습니다.

<br>

### Tile 배치하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_006.png" alt="MSW-100_Defense_1_006"
</p>

- 캐릭터 또는 유닛이 상호작용 할 지형을 배치하기 위해 TileMap에서 활용할 수 있는 Tile을 배치해야 합니다.
- 에디터 상단의 **Tile** - **Preset List**를 통해 TileMap에 배치할 수 있는 프리셋 목록을 확인합니다.

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_007.png" alt="MSW-100_Defense_1_007"
</p>

  1. 배치하고자 하는 지형 타일을 선택합니다.
  2. 맵 환경 내에 좌클릭 또는 드래그를 통해 자연스러운 지형 배치가 가능합니다.

<br>

## Player Model

<p align="center">
<img src="../../_images/MSW-100_Defense_1_008.png" alt="MSW-100_Defense_1_008"
</p>

- 월드 실행 시 나타나는 기본 캐릭터는 프로젝트 내에 등록된 모든 파일을 관리하는 **Workspace**에 등록된 Player Model을 기반으로 생성됩니다.
- Player Model의 다양한 속성을 관리해 월드 실행 시 나타나는 캐릭터의 세팅을 미리 준비해 둘 수 있습니다.

<br>  

### Player Model 컴포넌트 추가 및 삭제하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_009.png" alt="MSW-100_Defense_1_009"
</p>

- **Workspace** 탭에서 NativeModel 경로에 등록된 **Player Model**을 확인합니다.
- **Property** 탭에서 제거하고자 하는 컴포넌트를 우클릭 - Remove Component를 통해 기본 캐릭터의 컴포넌트를 미리 삭제해 둘 수 있습니다.
- 기본 캐릭터에 새로운 컴포넌트를 미리 등록해 두고자 할 땐, 하단의 **AddComponent**를 통해 새로운 컴포넌트를 추가할 수 있습니다.

<br>

### 캐릭터의 기본 컴포넌트 설정하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_018.png" alt="MSW-100_Defense_1_018"
</p>

- 디펜스 샘플월드에서는 캐릭터 역할이 필요가 없으므로 월드 환경 출력과 캐릭터 기본 컴포넌트를 제외한 모든 컴포넌트를 삭제해 줍니다.
- 필수적으로 등록되어야하는 컴포넌트는 다음과 같습니다.
  - CameraComponent, PlayerControllerComponent, PlayerComponent

<br>

### Player Model의 속성값 편집하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_010.png" alt="MSW-100_Defense_1_010"
</p>

1. **Workspace** 탭에서 **DefaultPlayer** 를 더블클릭해 Model 편집모드로 진입합니다.
2. 기본 캐릭터의 Model 엔티티를 선택한 후 Property 탭에서 원하는 컴포넌트 값을 설정할 수 있습니다.

<br>

## 기지 제작하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_012.png" alt="MSW-100_Defense_1_012"
</p>

- 본격적인 게임이 시작되면 플레이어는 적의 기지를, 적은 플레이어의 기지를 파괴해야 합니다.
- 유닛 생성의 근원지인 기지 엔티티를 배치해야 하며, 기지마다 수행해야 하는 기능들을 담는 컴포넌트를 제작해 등록합니다.

<br>

### 엔티티 생성하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_011.png" alt="MSW-100_Defense_1_011"
</p>

- 엔티티를 생성하고자 하는 경로 또는 부모 대상 엔티티를 **우클릭** - **Create Entity as Child** - **Create Empty** 하여 새로운 엔티티를 생성할 수 있습니다.
- 빈 엔티티를 생성한 경우 기본적으로 등록되는 컴포넌트는 엔티티의 위치를 결정하는 TransformComponent밖에 존재하지 않으므로 아무런 외형이 등록되어 있지 않습니다.

<br>

### 엔티티 외형 등록하기
<p align="center">
<img src="../../_images/MSW-100_Defense_1_013.png" alt="MSW-100_Defense_1_013"
</p>

- 새로 생성된 엔티티를 선택합니다.
- **Property** 탭에서 **Add Component**를 클릭합니다.
- **SpriteRenderComponent**를 선택해 컴포넌트를 등록합니다.

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_014.png" alt="MSW-100_Defense_1_014"
</p>

- 등록된 **SpriteComponent** 속성 중에서 SpriteRUID 옵션의 우측 '⦿'버튼을 클릭해 ResourcePicker 창을 활성화합니다.
- 원하는 리소스 이미지를 선택합니다.

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_015.png" alt="MSW-100_Defense_1_015"
</p>

- Sprite의 이미지를 **X축** 또는 **Y축**으로 반전하려면 SpriteComponent의 Flip 옵션을 체크해 이미지를 반전시킬 수 있습니다.

<br>

### BaseComponent 제작하기

<p align="center">
<img src="../../_images/MSW-100_Defense_1_016.png" alt="MSW-100_Defense_1_016"
</p>

- 새로운 컴포넌트 스크립트를 생성하기 위해 **Workspace**에서 생성하고자 하는 경로를 **우클릭** - **Create Scripts** - **Create Component**를 선택합니다.
- 스크립트가 수행할 기능과 목적에 맞는 이름으로 컴포넌트 명을 정의합니다.

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_017.png" alt="MSW-100_Defense_1_017"
</p>

- 기지 역할을 수행할 엔티티를 선택한 다음 **Add Component** 를 통해 새로 생성한 BaseComponent를 등록해 줍니다.

<br>

## BaseComponent 코드 작성하기
- 다음과 같이 코드를 작성해야 합니다.
- 해당 코드는 Plain Text 환경으로 작성되었습니다.

```lua
@Component
script BaseComponent extends Component

property boolean isUser = true -- 해당 기지가 유저의 소유인지 나타내는 변수입니다.

property integer hp = 0 -- 해당 기지의 현재 체력값을 나타냅니다.

property Entity spawnLocation = nil

property number gold = 0 -- 유닛을 생산할 수 있는 재화 값입니다.

property number goldTimer = 0 -- 일정 시간마다 재화를 생산하기 위해 측정하는 타이머입니다.

property number goldPerSecond = 0 -- 초당 몇 골드를 생산할지 결정하는 변수입니다.

property string nextSpawnUnitName = "" -- 다음으로 생산되어야 하는 유닛입니다.(Enemy 전용 변수)

property integer needGold = 0 -- 행동 시 필요한 재화값을 나타냅니다.

@ExecSpace("ClientOnly")
method void OnBeginPlay()
-- 월드 실행 시 바로 호출되는 함수입니다.

-- 자신의 초기화를 진행합니다.
self:Init()
end

@ExecSpace("ClientOnly")
method void Init()
-- 컴포넌트의 초기화를 수행합니다.

-- 자식엔티티 중 유닛 스폰 위치를 결정할 'SpawnLocation' 엔티티를 찾아 등록합니다.
self.spawnLocation = self.Entity:GetChildByName("SpawnLocation")

-- 현재의 재화값을 0으로 초기화합니다.
self.gold = 0
-- 재화를 생산하기 위한 타이머의 값을 0으로 설정합니다.
self.goldTimer = 0

-- 해당 기지가 유저의 것이라면,
if self.isUser == true then
	-- 초당 재화 생산량을 StageData의 PlayerGoldPerSecond값으로 설정합니다. (현재 스테이지 번호를 기준으로 합니다.)
	self.goldPerSecond = _DataSetLogic.stageData[_StageLogic.currentStage].PlayerGoldPerSecond
	-- 플레이어가 지켜야 하는 기지의 초기 체력값을 설정합니다.
	self.hp = _DataSetLogic.stageData[_StageLogic.currentStage].PlayerBaseHp
	
	_UI_MainGame:UpdateGoldText(self.gold)
	
-- 해당 기지가 적의 것이라면
else
	-- 초당 재화 생산량을 StageData의 EnemyGoldPerSecond값으로 설정합니다. (현재 스테이지 번호를 기준으로 합니다.) 
	self.goldPerSecond = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyGoldPerSecond
	-- 다음으로 생산해야할 유닛을 결정합니다.
	self.nextSpawnUnitName = self:SelectRandomUnitForEnemyName()
	-- 적이 지켜야 하는 기지의 초기 체력값을 설정합니다.
	self.hp = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyBaseHp
	-- 다음으로 생산해야할 유닛의 필요한 재화량 값을 기록합니다.
	self.needGold = _DataSetLogic.unitData[self.nextSpawnUnitName].Cost
end
end

@ExecSpace("ClientOnly")
method void SpawnUnit(string unitName)
-- 유닛을 생성하는 함수입니다.

-- 매개변수 1 unitName : 생산하는 유닛의 이름값을 전달 받습니다.

-- 오브젝트 풀로부터 유닛 엔티티를 반환받도록 요청합니다.
-- 전달받는 엔티티의 부모를 자신으로 설정하도록 매개변수를 전달합니다.
local unit = _ObjectPoolLogic:GetUnit(self.Entity)

-- 유닛 엔티티의 위치 좌표를 자식 엔티티인 SpawnLocaion의 위치와 동일하게 설정합니다.
unit.TransformComponent.Position = self.spawnLocation.TransformComponent.Position
-- Unit의 UnitComponet에 접근하여 초기화를 진행시킵니다.
unit.UnitComponent:Init(unitName,self.isUser)
end

@ExecSpace("ClientOnly")
method void OnDamage(integer damageValue)
-- 상대방 유닛으로 인한 피격시 호출되는 함수입니다.

-- 매개변수 1 damageValue : 상대방으로부터 얼마나의 피격 데미지를 전달받는지 결정합니다.

-- 체력값을 감소시킵니다.
self.hp -= damageValue

-- 체력이 0 이하라면
if self.hp <= 0 then
	-- 파괴 기능을 수행합니다.
	self:Destory()
	-- 스테이지의 진행이 종료되었음을 설정합니다.
	_StageLogic:SetStageState(false)
end
end

@ExecSpace("ClientOnly")
method void Destory()
-- 해당 엔티티의 파괴를 수행하는 함수입니다.

--파괴된 엔티티가 유저의 기지라면 패배라는 결과를 출력하는 팝업정보를 띄웁니다.
_UI_Result:ShowWithWinInfo(not self.isUser)

-- 컴포넌트가 부착된 엔티티를 비활성화 합니다.
self.Entity.Enable = false
end

@ExecSpace("ClientOnly")
method void OnUpdate(number delta)
-- 매 프레임마다 호출되는 함수입니다.

-- 스테이지가 종료되었다면
if _StageLogic.isStageStart == false then
	-- 이후의 기능을 수행하지 않고 종료합니다.
	return
end

-- 게임이 진행되는 동안 재화가 자동으로 축적되도록 합니다.
self:AutoGetGold(delta)
end

@ExecSpace("ClientOnly")
method void AutoGetGold(number delta)
-- 자동으로 재화를 축적하는 함수입니다.

-- 매개변수 1 delta : 프레임간 시간변화량의 값을 전달받습니다.

-- 1초의 단위를 측정하기 위한 타이머값을 증가시킵니다.
self.goldTimer += delta

-- 현재 스테이지의 소지 가능한 최대 재화량 값을 기록합니다.
-- ---@ type을 통해 해당 자료형이 정수임을 설정합니다.
---@ type integer
local maxGold = _DataSetLogic.stageData[_StageLogic.currentStage].MaxGold

-- 만약 1초 이상인 상태라면
if self.goldTimer >= 1 and self.gold < maxGold then
	-- 타이머를 0으로 다시 초기화합니다.
	self.goldTimer = 0
	-- 설정된 초당 골드값을 현재 골드값에 추가합니다.
	self.gold += self.goldPerSecond
	self.gold = math.clamp(self.gold,0,maxGold)
	
		-- 만약 해당 Base가 적의 기지라면
		if self.isUser == false then
			-- 자동 유닛 스폰함수를 동작하도록 합니다.
			self:EnemyAutoSpawn()
		-- 만약 해당 Base가 유저의 기지라면
	elseif self.isUser == true then
			-- 재화 정보를 나타내는 UI 텍스트의 값을 갱신하도록 합니다.
			_UI_MainGame:UpdateGoldText(self.gold)
		end
end
end

@ExecSpace("ClientOnly")
method string SelectRandomUnitForEnemyName()
-- StageData에 등록된 현재 스테이지에 맞는 EnemyUnit중 랜덤한 유닛 이름을 가져오는 함수입니다.

-- 유닛들의 이름을 담아둘 테이블을 생성합니다.
local enemyUnits = {} 
-- DataSetLogic으로부터 현재 스테이지에서 생성할 수 있는 유닛 이름 테이블을 그대로 가져옵니다.
enemyUnits = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyUnit

-- 테이블의 1번요소부터 테이블의 끝 요소중 랜덤한 인덱스 값을 생성합니다.
local selectRandomIndex = _UtilLogic:RandomIntegerRange(1,#enemyUnits)

-- 해당 인덱스에 맞는 유닛의 이름을 가져옵니다.
local randomUnitName = enemyUnits[selectRandomIndex]

-- 해당 유닛의 이름을 반환합니다.
return randomUnitName
end

@ExecSpace("ClientOnly")
method void EnemyAutoSpawn()
-- 적 기지 전용 함수로, 자동으로 유닛을 생성합니다.

-- 만약 적 기지의 골드값이 다음 유닛생성에 필요한 골드값만큼 충족하다면
if self.gold >= self.needGold then
	-- 필요한 유닛을 스폰하도록 합니다.
	self:SpawnUnit(self.nextSpawnUnitName)
	-- 현재 재화에서 필요한 재화값을 감소시킵니다.
	self.gold -= self.needGold
	-- 다음으로 생성할 유닛을 다시 선택합니다.
	self.nextSpawnUnitName = self:SelectRandomUnitForEnemyName()
end
end

@ExecSpace("ClientOnly")
method void PlayerRequestSpawn(string unitName)
-- 매개변수로 전달받는 유닛을 스폰해달라고 요청하는 함수입니다.(플레이어 전용 함수)

-- 매개변수 1 unitName : 스폰하고자 하는 유닛의 유닛명

-- 대상 유닛의 생산 비용값을 확인합니다.
local targetUnitCost = _DataSetLogic.unitData[unitName].Cost

-- 현재 소지 골드값이 대상 유닛의 생산 비용보다 높다면
if self.gold >= targetUnitCost then
	-- 대상 유닛을 그대로 스폰하도록 요청합니다.
	self:SpawnUnit(unitName)
	-- 현재 재화량에서 유닛의 생산비용만큼 재화량을 감소시킵니다.
	self.gold -= targetUnitCost
	-- 재화량을 나타내는 TextUI를 소모된 현재 재화량으로 새로 갱신합니다.
	_UI_MainGame:UpdateGoldText(self.gold)
end
end

@ExecSpace("ClientOnly")
method void ReturnAllUnit()
-- 생산된 유닛을 전부 오브젝트 풀로 다시 반환하는 함수입니다.

-- 자식 엔티티중에서 UnitComponent가 등록된 엔티티들을 Table로 기록하도록 합니다.
local units = self.Entity:GetChildComponentsByTypeName("UnitComponent")

-- 테이블을 전부 순회합니다.
for _, unit in pairs(units) do
	-- 오브젝트 풀에 해당 유닛을 반환하도록 합니다.
	_ObjectPoolLogic:ReturnUnit(unit.Entity)
end
end

end
```

<br>

## 최소 구현 기준
- 메이플스토리 월드에서 제공되는 리소스를 활용하기 위해 'Maple TileMap' 환경으로 설정되어야 합니다.
- 게임 종료의 트리거인 기지 파괴를 수행하기 위해 기본적인 기지 엔티티를 배치해야 하며 BaseComponent를 등록해 기지의 역할을 수행해야 합니다.

<br>

## 응용 및 확장 방향
- 적(인공지능)의 기지를 추가로 배치하여 게임의 난이도를 훨씬 어렵게 설정할 수 있습니다.
- 맵 환경 내에 다양한 조형물 엔티티를 배치하여 더욱 게임을 다채롭게 제작할 수 있습니다.


















