# [💡基礎學習] 製作世界環境
<br>
<br>
<br>

## 影片時間軸
- 可在課程影片中以下時間點學習。
- 建立世界環境：00:00:00

<br>

## 學習目標
- 學習楓之谷世界最基本的地圖方式Maple TileMap模式。
- 可以在TileMap模式中配置圖塊地圖，建立遊戲環境。
- 分析防禦類型的特性，並可自行組成遊戲製作環境。
- 可以在世界的地圖內配置各種實體。

<br>

## 觀看該影片後可嘗試執行的內容
- 製作並配置基地以外的背景元素實體。
- 控制基地實體TransformComponent的屬性值，製作更大或更小的基地。

<br>

##楓之谷世界地圖模式
<p align="Center">
<img src="../../_images/MSW-100_Defense_1_001_tw.png" alt="MSW-100_Defense_1_001_tw"
</p>

<br>

### TileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_002.png" alt="MSW-100_Defense_1_002"
</p>

- 在楓之谷世界中初次建立世界時預設指定的地圖形式。
- 是最能展現楓之谷風格的世界方式。
- 利用楓之谷世界提供的Tile生成地面，並配置實體來形成地圖。
- 可依圖塊的配置製作斜坡。
- 在製作橫向捲軸形式的遊戲時，是能展現楓之谷風格的有用地圖方式。

<br>

### RectTileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_003.png" alt="MSW-100_Defense_1_003"
</p>

- 在製作俯視視角形式的遊戲時，是有效的地圖形式。
- 可自行製作圖塊集，或使用楓之谷世界提供的圖塊集資源來形成地圖。
- 可透過棋盤格形式配置圖塊來形成地圖。
- 可為每個圖塊賦予可移動與不可移動的性質。
- 與TileMap、SideViewRectTileMap方式不同，可製作4方向、8方向移動形式的遊戲。

<br>

### SideviewRectTileMap
<p align="center">
<img src="../../_images/MSW-100_Defense_1_004.png" alt="MSW-100_Defense_1_004"
</p>

- 在製作橫向捲軸形式遊戲時，是有效的地圖形式。
- 可自行製作圖塊集，或使用楓之谷世界提供的圖塊集資源來形成地圖。
- 可透過棋盤格形式配置圖塊來形成地圖。
- 與RectTileMap不同，若不是不可移動的圖塊集，角色會**穿過**圖塊。
- 因此使用SideViewRectTileMap形成地圖時，必須將不可移動的圖塊集製作為**地面**。

<br>

## 配置地形

### 選擇TileMap模式
<p align="center">
<img src="../../_images/MSW-100_Defense_1_005.png" alt="MSW-100_Defense_1_005"
</p>

- 長按編輯器上方的「圖塊編輯器按鈕」時，會顯示可選擇地圖模式的選單列表。
- 選擇列表中顯示的「MapleTile」地圖模式。

ℹ️ 製作新世界後，若未設定任何環境，基本地圖模式會設定為「MapleTile」。

<br>

### 配置Tile
<p align="center">
<img src="../../_images/MSW-100_Defense_1_006.png" alt="MSW-100_Defense_1_006"
</p>

- 為了配置角色或英雄會互動的地形，必須配置可在TileMap中運用的Tile。
- 透過編輯器上方的**Tile** - **Preset List**確認可配置於TileMap的預設列表。

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_007.png" alt="MSW-100_Defense_1_007"
</p>

  1.選擇想要配置的地形圖塊。
  2.在地圖環境內透過左鍵點擊或拖曳，即可自然配置地形。

<br>

## Player Model

<p align="center">
<img src="../../_images/MSW-100_Defense_1_008.png" alt="MSW-100_Defense_1_008"
</p>

- 執行世界時出現的基本角色，會根據登錄於管理專案內所有檔案的**Workspace**中的Player Model生成。
- 管理Player Model的各種屬性，即可預先準備執行世界時出現之角色的設定。

<br>  

### 新增及刪除Player Model元件
<p align="center">
<img src="../../_images/MSW-100_Defense_1_009.png" alt="MSW-100_Defense_1_009"
</p>

- 在**Workspace**分頁中，確認NativeModel路徑中登錄的**Player Model**。
- 可在**Property**分頁中，透過右鍵點擊想移除的元件 - Remove Component，預先刪除基本角色的元件。
- 若想預先在基本角色上登錄新的元件，可透過下方的**AddComponent**新增元件。

<br>

### 設定角色的基本元件
<p align="center">
<img src="../../_images/MSW-100_Defense_1_018.png" alt="MSW-100_Defense_1_018"
</p>

- 在防禦範例世界中不需要角色功能，因此會刪除世界環境輸出與角色基本元件以外的所有元件。
- 必須登錄的元件如下。
  - CameraComponent, PlayerControllerComponent, PlayerComponent

<br>

### 編輯Player Model的屬性值
<p align="center">
<img src="../../_images/MSW-100_Defense_1_010.png" alt="MSW-100_Defense_1_010"
</p>

1.**Workspace**分頁中雙擊**DefaultPlayer**，進入Model編輯模式。
2.選擇基本角色的Model實體後，可在Property分頁中設定想要的元件值。

<br>

## 製作基地
<p align="center">
<img src="../../_images/MSW-100_Defense_1_012.png" alt="MSW-100_Defense_1_012"
</p>

- 正式開始遊戲後，玩家必須破壞敵方基地，敵人則必須破壞玩家基地。
- 必須配置作為英雄生成來源的基地實體，並製作、登錄包含各基地應執行功能的元件。

<br>

###建立實體
<p align="center">
<img src="../../_images/MSW-100_Defense_1_011.png" alt="MSW-100_Defense_1_011"
</p>

- 可在想生成實體的路徑或父對象實體上**右鍵點擊** - **Create Entity as Child** - **Create Empty**，生成新的實體。
- 生成空實體時，基本登錄的元件只有決定實體位置的TransformComponent，因此不會登錄任何外型。

<br>

### 登錄實體外型
<p align="center">
<img src="../../_images/MSW-100_Defense_1_013.png" alt="MSW-100_Defense_1_013"
</p>

- 選擇新生成的實體。
- 在**Property**分頁中點擊**Add Component**。
- 選擇**SpriteRenderComponent**登錄元件。

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_014.png" alt="MSW-100_Defense_1_014"
</p>

- 在登錄的**SpriteComponent**屬性中，點擊SpriteRUID選項右側的「⦿」按鈕，啟用ResourcePicker視窗。
- 選擇想要的資源圖片。

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_015.png" alt="MSW-100_Defense_1_015"
</p>

- 若要將Sprite圖片以**X軸**或**Y**軸反轉，可以勾選SpriteComponent的Flip選項來反轉圖片。

<br>

### 製作BaseComponent

<p align="center">
<img src="../../_images/MSW-100_Defense_1_016.png" alt="MSW-100_Defense_1_016"
</p>

- 為了生成新的元件腳本，在**Workspace**中對想生成的路徑**右鍵點擊** - **Create Scripts** - **Create Component**。
- 依腳本要執行的功能與目的，定義元件名稱。

<br>
<br>

<p align="center">
<img src="../../_images/MSW-100_Defense_1_017.png" alt="MSW-100_Defense_1_017"
</p>

- 選擇要執行基地角色的實體後，透過**Add Component**登錄新生成的BaseComponent。

<br>

## 撰寫BaseComponent程式碼
- 必須如下撰寫程式碼。
- 該程式碼是在Plain Text環境下撰寫。

```lua
@Component
script BaseComponent extends Component

property boolean isUser = true -- 表示該基地是否為使用者所有的變數。

property integer hp = 0 -- 表示該基地目前體力值。

property Entity spawnLocation = nil

property number gold = 0 -- 可生產英雄的資源值。

property number goldTimer = 0 -- 為了每隔固定時間生產資源而測量的計時器。

property number goldPerSecond = 0 -- 決定每秒生產多少黃金的變數。

property string nextSpawnUnitName = "" -- 下一個應生產的英雄。(Enemy專用變數)

property integer needGold = 0 -- 表示行動時所需資源值。

@ExecSpace("ClientOnly")
method void OnBeginPlay()
-- 世界執行時立即呼叫的函式。

-- 進行自身初始化。
self:Init()
end

@ExecSpace("ClientOnly")
method void Init()
-- 執行元件初始化。

-- 在子實體中尋找並登錄決定英雄生成位置的「SpawnLocation」實體。
self.spawnLocation = self.Entity:GetChildByName("SpawnLocation")

-- 將目前資源值初始化為0。
self.gold = 0
-- 將用於生產資源的計時器值設定為0。
self.goldTimer = 0

-- 若該基地屬於使用者，
if self.isUser == true then
	-- 將每秒資源生產量設定為StageData的PlayerGoldPerSecond值。(以目前關卡編號為基準。)
	self.goldPerSecond = _DataSetLogic.stageData[_StageLogic.currentStage].PlayerGoldPerSecond
	-- 設定玩家必須守護的基地初始體力值。
	self.hp = _DataSetLogic.stageData[_StageLogic.currentStage].PlayerBaseHp
	
	_UI_MainGame:UpdateGoldText(self.gold)
	
-- 若該基地屬於敵方
else
	-- 將每秒資源生產量設定為StageData的EnemyGoldPerSecond值。(以目前關卡編號為基準。) 
	self.goldPerSecond = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyGoldPerSecond
	-- 決定下一個應生產的英雄。
	self.nextSpawnUnitName = self:SelectRandomUnitForEnemyName()
	-- 設定敵方必須守護的基地初始體力值。
	self.hp = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyBaseHp
	-- 記錄下一個應生產英雄所需的資源量值。
	self.needGold = _DataSetLogic.unitData[self.nextSpawnUnitName].Cost
end
end

@ExecSpace("ClientOnly")
method void SpawnUnit(string unitName)
-- 生成英雄的函式。

-- 參數1 unitName：接收要生產英雄的名稱值。

-- 請求從物件池接收英雄實體。
-- 傳遞參數，使收到的實體父物件設定為自己。
local unit = _ObjectPoolLogic:GetUnit(self.Entity)

-- 將英雄實體的位置座標設定為與子實體SpawnLocaion的位置相同。
unit.TransformComponent.Position = self.spawnLocation.TransformComponent.Position
-- 存取Unit的UnitComponet並進行初始化。
unit.UnitComponent:Init(unitName,self.isUser)
end

@ExecSpace("ClientOnly")
method void OnDamage(integer damageValue)
-- 因對方英雄造成受擊時呼叫的函式。

-- 參數1 damageValue：決定從對方接收多少受擊傷害。

-- 減少體力值。
self.hp -= damageValue

-- 若體力為0以下
if self.hp <= 0 then
	-- 執行破壞功能。
	self:Destory()
	-- 設定關卡進行已結束。
	_StageLogic:SetStageState(false)
end
end

@ExecSpace("ClientOnly")
method void Destory()
-- 執行該實體破壞的函式。

-- 若被破壞的實體是使用者基地，則顯示輸出敗北結果的彈出視窗資訊。
_UI_Result:ShowWithWinInfo(not self.isUser)

-- 停用附加元件的實體。
self.Entity.Enable = false
end

@ExecSpace("ClientOnly")
method void OnUpdate(number delta)
-- 每一格呼叫的函式。

-- 若關卡已結束
if _StageLogic.isStageStart == false then
	-- 不執行後續功能並結束。
	return
end

-- 讓資源在遊戲進行期間自動累積。
self:AutoGetGold(delta)
end

@ExecSpace("ClientOnly")
method void AutoGetGold(number delta)
-- 自動累積資源的函式。

-- 參數1 delta：接收格與格之間時間變化量的值。

-- 增加用於測量1秒單位的計時器值。
self.goldTimer += delta

-- 記錄目前關卡可擁有的最大資源量值。
-- 透過---@ type設定該資料型態為整數。
---@ type integer
local maxGold = _DataSetLogic.stageData[_StageLogic.currentStage].MaxGold

-- 若狀態為1秒以上
if self.goldTimer >= 1 and self.gold < maxGold then
	-- 將計時器重新初始化為0。
	self.goldTimer = 0
	-- 將設定的每秒Gold值追加到目前Gold值。
	self.gold += self.goldPerSecond
	self.gold = math.clamp(self.gold,0,maxGold)
	
		-- 若該Base是敵方基地
		if self.isUser == false then
			-- 執行自動英雄生成函式。
			self:EnemyAutoSpawn()
		-- 若該Base是使用者基地
	elseif self.isUser == true then
			-- 更新表示資源資訊的UI文字值。
			_UI_MainGame:UpdateGoldText(self.gold)
		end
end
end

@ExecSpace("ClientOnly")
method string SelectRandomUnitForEnemyName()
-- 從StageData中登錄的符合目前關卡EnemyUnit中，取得隨機英雄名稱的函式。

-- 生成用來存放英雄名稱的表格。
local enemyUnits = {} 
-- 從DataSetLogic中原樣取得目前關卡可生成的英雄名稱表格。
enemyUnits = _DataSetLogic.stageData[_StageLogic.currentStage].EnemyUnit

-- 從表格第1個元素到表格最後一個元素中生成隨機索引值。
local selectRandomIndex = _UtilLogic:RandomIntegerRange(1,#enemyUnits)

-- 取得符合該索引的英雄名稱。
local randomUnitName = enemyUnits[selectRandomIndex]

-- 回傳該英雄名稱。
return randomUnitName
end

@ExecSpace("ClientOnly")
method void EnemyAutoSpawn()
-- 敵方基地專用函式，自動生成英雄。

-- 若敵方基地的黃金值滿足下一個英雄生成所需黃金值
if self.gold >= self.needGold then
	-- 生成所需英雄。
	self:SpawnUnit(self.nextSpawnUnitName)
	-- 從目前資源中扣除所需資源值。
	self.gold -= self.needGold
	-- 重新選擇下一個要生成的英雄。
	self.nextSpawnUnitName = self:SelectRandomUnitForEnemyName()
end
end

@ExecSpace("ClientOnly")
method void PlayerRequestSpawn(string unitName)
-- 請求生成作為參數傳入的英雄的函式。(玩家專用函式)

-- 參數1 unitName：想生成英雄的英雄名稱

-- 確認目標英雄的生產費用值。
local targetUnitCost = _DataSetLogic.unitData[unitName].Cost

-- 若目前擁有黃金值高於目標英雄的生產費用
if self.gold >= targetUnitCost then
	-- 請求原樣生成目標英雄。
	self:SpawnUnit(unitName)
	-- 從目前資源量中扣除英雄生產費用的資源量。
	self.gold -= targetUnitCost
	-- 將顯示資源量的TextUI更新為消耗後的目前資源量。
	_UI_MainGame:UpdateGoldText(self.gold)
end
end

@ExecSpace("ClientOnly")
method void ReturnAllUnit()
-- 將生產出的所有英雄重新歸還至物件池的函式。

-- 將子實體中登錄了UnitComponent的實體記錄為Table。
local units = self.Entity:GetChildComponentsByTypeName("UnitComponent")

-- 巡覽整個表格。
for _, unit in pairs(units) do
	-- 將該英雄歸還至物件池。
	_ObjectPoolLogic:ReturnUnit(unit.Entity)
end
end

end
```

<br>

## 最低製作標準
- 為了運用楓之谷世界提供的資源，必須設定為「Maple TileMap」環境。
- 為了執行作為遊戲結束觸發器的基地破壞，必須配置基本基地實體，並登錄BaseComponent使其執行基地角色。

<br>

## 應用與進階應用
- 可以額外配置敵方(人工智慧)基地，將遊戲難度設定得更高。
- 可以在地圖環境內配置各種造型物實體，將遊戲製作得更加豐富。


















