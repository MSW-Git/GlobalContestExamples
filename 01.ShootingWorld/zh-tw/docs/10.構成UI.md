# [💡基礎學習]構成UI
<br>
<br>
<br>

## 影片時間軸
- 可在課程影片中以下時間點進行學習。
- 實作UI：03:00:20
<br>

## 學習目標
- 學習在楓之谷世界中構成UI的方法。
- 學習Localizing功能，並可學習套用Localizing的方法。
- 理解UIManageLogic的角色，並學習活用方法。
- 可參考範例世界的程式碼，製作符合欲製作遊戲的UI並賦予功能。
<br>

## 觀看該影片後可嘗試執行的內容
- 直接實作範例世界中已實作的功能。
- 在實作過程中，變更UI以展現所製作世界的特色。
- 製作並管理新的UIGroup，新增UI以傳遞所需資訊。
<br>

## 理解UI Group

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_001.png" alt="MSW-100_Shooting_10_001.png">
</p>

- 查看Hierarchy視窗下方，可以確認到以ui這個名稱構成的實體。
- 該實體是為了輸出ui而構成的ui group。
- 可透過修改ui group來變更或製作要輸出的ui。

<br>

## 建立UI Group

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_002.png" alt="MSW-100_Shooting_10_002.png">
</p>

- 點擊編輯器上方的UI按鈕，切換為UI編輯狀態。

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_003.png" alt="MSW-100_Shooting_10_003.png">
</p>

- 可在編輯器視窗下方的UI Groups中點擊+按鈕來新增新的UI Groups。
- 若有需要刪除的UI Groups，可點擊要刪除的UI Group後，再點擊垃圾桶按鈕進行刪除。

<br>

## 製作空的UI實體

- 在UI中也可以像製作一般實體一樣製作實體。
- 差異在於，建立空實體時不是使用Create Empty，而是使用Create UIEmpty。

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_004.png" alt="MSW-100_Shooting_10_004.png">
</p>

- 在Hierarchy視窗中對要新增實體的ui group按右鍵。
- 點擊Create Entity。
點擊Create UIEmpty即可建立空的UI實體。

<br>

## 為UI實體賦予Component

- UI實體也和一般實體一樣，可以賦予Component來執行額外功能。
- 賦予Component的方法與一般實體使用Add Component的方式相同。
- 在本單元中，將說明常用的Component。
    - **SpriteGUIRendererComponent**：這是用於在UI中顯示圖片的Component。在UI中輸出圖片時，比起SpriteRendererComponent，更建議使用該Component。
    - **TextGUIRendererComponent**：這是用於在UI中顯示文字的Component。
    - **ButtonComponent**：當UI需要執行按鈕功能時所使用的Component。

<br>

## 為UIGroup賦予Controller
- Controller這個概念，是透過製作Component來管理該UI的概念。
- 為每個UIGroup賦予Controller Component，並構成功能，讓該Component處理與UI相關的功能。
- 將各Controller登錄到UIManageLogic中，當呼叫UIManageLogic時，就能向其他UI請求與UI相關的功能。
- 在範例世界中，製作了如下的UIController。
    - **DefaultUIController**：這是用來管理DefaultUI的Component。主要負責更新玩家體力、技能使用次數，也負責在遊戲開始時將玩家的基本攻擊與技能使用功能登錄到各按鈕。
    - **TitleUIController**：這是用來構成標題畫面的Component。該Component是為了處理開始遊戲按鈕與輸出遊戲玩法UI的功能而製作。
    - **ResultUI**：當GameLogic將遊戲結果傳遞給UIManageLogic時，UIManageLogic會向該Component請求應顯示的結果UI。 

<br>

## 製作Localizing DataSet

- Localizing功能是在需要服務其他國家時的必要功能。
- 楓之谷世界可利用Locale DataSet來製作該功能。

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_005.png" alt="MSW-100_Shooting_10_005.png">
</p>

- 在Workspace中對MyDesk按右鍵。
- 點擊Create LocaleDataSet。
- 點擊Default來製作Locale DataSet。
- 在範例世界中，是以StringTable製作。

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_006.png" alt="MSW-100_Shooting_10_006.png">
</p>

<table class="tg">
  <thead>
    <tr>
      <th class="tg-c3ow">姓名</th>
      <th class="tg-c3ow">說明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td class="tg-lboi">Key</td>
      <td class="tg-lboi">這是楓之谷世界在進行Localizing時，用來尋找需要Localizing文字的ID值。</td>
    </tr>
    <tr>
      <td class="tg-lboi">Source</td>
      <td class="tg-lboi">這是用來承載翻譯原文文字的值。若使用楓之谷世界提供的自動翻譯功能，便會依據該值尋找翻譯值。</td>
    </tr>
    <tr>
      <td class="tg-lboi">Note</td>
      <td class="tg-lboi">這是用來記載該文字是以什麼意義、用途撰寫的欄位。</td>
    </tr>
    <tr>
      <td class="tg-lboi">ko</td>
      <td class="tg-lboi">這是用來承載韓文輸出文字的值。</td>
    </tr>
    <tr>
      <td class="tg-lboi">en</td>
      <td class="tg-lboi">這是用來承載英文輸出文字的值。</td>
    </tr>
  </tbody>
</table>

- 此外，也可以加入楓之谷世界官方支援的各種語言來製作。

<br>

## 套用Localizing

- 套用Localizing功能的方法大致有2種。
- 一種是利用TextGUIRendererComponent的值，另一種則是透過腳本使用。

### 利用TextGUIRendererComponent的方法

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Shooting/MSW-100_Shooting_10_007.png" alt="MSW-100_Shooting_10_007.png">
</p>

- 在製作完成的Locale DataSet中輸入要套用Localizing的值
- 在TextGUIRendererComponent的Text值中輸入Locale DataSet的Key。
- 將TextGUIRendererComponent的isLocalizationKey值設為勾選狀態。
- 透過該狀態，可以讓Localizing功能自動套用。

<br>

### 利用腳本的方法

- 在範例世界中，為了方便快速使用，已先製作好LocalizeLogic。
- 以下是已製作的LocalizeLogic程式碼。
- 該程式碼以Plain Text撰寫。

```lua
@Logic
script LocalizeLogic extends Logic

@ExecSpace("ClientOnly")
method string GetLocalizeStringByKey(string StringKey)
-- 依據傳入的StringKey值，尋找並回傳StringTable中的值。
return _LocalizationService:GetText(StringKey)
end

end
```

- 當以參數StringKey傳入Locale DataSet的Key值時，會依照目前遊戲執行國家的語言回傳文字。
    - EX：若遊玩國家為美國等英語圈，則會回傳en的值。

<br>

## 活用UIManageLogic

- 當所有UI設定都完成後，為了管理各Controller，必須將其登錄到UIManageLogic中並設定功能。
- 以下是UIManageLogic的程式碼。
- 該程式碼是在Plain Text環境中撰寫。

```lua
@Logic
script UIManageLogic extends Logic

property TitleUIController titleUIController = nil

property SelectCharacterUIController selectCharacterUIController = nil

property ResultUIController resultUIController = nil

property DefaultUIController defaultUIController = nil

@ExecSpace("ClientOnly")
method void OnBeginPlay()
-- 當遊戲開始時，尋找並登錄TitleUIController。
self:InitTitleUIController()
-- 當遊戲開始時，尋找並登錄SelectCharacterUIController。
self:InitSelectCharacterUIController()
-- 當遊戲開始時，尋找並登錄ResultUIController。
self:InitResultUIController()
-- 當遊戲開始時，尋找並登錄DefaultUIController。
self:InitDefaultUIController()
end

@ExecSpace("ClientOnly")
method void InitTitleUIController()
--若未登錄TitleUIController，則登錄該元件。
if not isvalid(self.titleUIController) then
	local titleUIEntity = _EntityService:GetEntityByPath("/ui/TitleUI")
	--若成功找到titleUIGroup，則登錄該實體的元件。
	if isvalid(titleUIEntity) then
		local component = titleUIEntity.TitleUIController
		-- 搜尋是否已正常登錄元件後，將其指派。
		if isvalid(component) then
			self.titleUIController = component
		end 
	end
end
end

@ExecSpace("ClientOnly")
method void InitSelectCharacterUIController()
--若未登錄SelectCharacterUIController，則登錄該元件。
if not isvalid(self.selectCharacterUIController) then
	local selectCharacterUIEntity = _EntityService:GetEntityByPath("/ui/SelectCharacterUI")
	--若成功找到SelectCharacterUIController，則登錄該實體的元件。
	if isvalid(selectCharacterUIEntity) then
		local component = selectCharacterUIEntity.SelectCharacterUIController
		-- 搜尋是否已正常登錄元件後，將其指派。
		if isvalid(component) then
			self.selectCharacterUIController = component
		end 
	end
end
end

@ExecSpace("Client")
method void ShowTitle()
-- 顯示標題畫面。
self.titleUIController:Show()
-- 其餘UI全部設為隱藏。
self.selectCharacterUIController:Hide()
self.resultUIController:Hide()
self.defaultUIController:Hide()
end

@ExecSpace("Client")
method void CallSelectCharacterUI()
-- 顯示角色選擇視窗。
self.selectCharacterUIController:Show()
-- 嘗試初始化角色選擇視窗內的角色清單。
self.selectCharacterUIController:InitCharacterSelectFrame()
end

@ExecSpace("Client")
method void ShowInGame()
-- 初始化作為遊戲畫面UI的defaultUI。
self.defaultUIController:Show()
-- 請求初始化自動攻擊UI。
self.defaultUIController:UpdateAutoUI()
-- 將其餘UI設為隱藏。
self.titleUIController:Hide()
self.selectCharacterUIController:Hide()
self.resultUIController:Hide()
end

@ExecSpace("Client")
method void InitResultUIController()
--若尚未登錄ResultUIController，則登錄該元件。
if not isvalid(self.resultUIController) then
	local resultUIEntity = _EntityService:GetEntityByPath("/ui/ResultUI")
	--若成功找到ResultUIController，則登錄該實體的元件。
	if isvalid(resultUIEntity) then
		local component = resultUIEntity.ResultUIController
		-- 搜尋是否已正常登錄元件後，將其指派。
		if isvalid(component) then
			self.resultUIController = component
		end 
	end
end
end

@ExecSpace("Client")
method void ShowResultUI(boolean IsClear)
-- 顯示結果UI。
self.resultUIController:Show(IsClear)
if IsClear then
	--播放祝賀效果音
	_SoundService:PlaySound("5d57b617cfc242cca347925e209c8e05", 1.0)
else
	--播放失敗效果音
	_SoundService:PlaySound("52dccfb8889b4181b9181780dc7e9040", 1.0)
end
end

@ExecSpace("Client")
method void InitDefaultUIController()
--若尚未登錄defaultUIController，則登錄該元件。
if not isvalid(self.defaultUIController) then
	local titleUIEntity = _EntityService:GetEntityByPath("/ui/DefaultUI")
	--若成功找到DefaultUIGroup，則登錄該實體的元件。
	if isvalid(titleUIEntity) then
		local component = titleUIEntity.DefaultUIController
		-- 搜尋是否已正常登錄元件後，將其指派。
		if isvalid(component) then
			self.defaultUIController = component
		end 
	end
end
end

@ExecSpace("Client")
method void UpdateLifeCountUI(number PlayerLife)
-- 依照PlayerLife數量更新DefaultUI的體力UI。
self.defaultUIController:UpdateLifeUI(PlayerLife)
end

@ExecSpace("Client")
method void UpdateSkillCountUI(number LeftSkillCount)
-- 依照LeftSkillCount的數量更新DefaultUI的技能UI。
self.defaultUIController:UpdateSkillUI(LeftSkillCount)
end

end
```

- 已註冊所有UI Group的Controller，並依照需要的UI更新製作對應方法。
- 當GameLogic或PlayerState有UI更新請求時，呼叫負責的Controller來執行該作業。
<br>

## 最低實作標準
- 建構管理UI的Controller，並建立UIManageLogic，使Controller功能能在外部方便使用。
- 在UI中，當按鈕等元素被點擊時，必須產生互動。
- UI必須傳遞遊戲進行所需的資訊。
<br>

## 應用與擴展方向
- 當玩家受到攻擊時，製作Event讓UI無需向UIManageLogic請求更新也能自動更新。
	- 此方式將成為日後製作Server與Client通訊時同步處理的基礎。
- 可依需求製作新的UI或在畫面上加入效果，讓使用者能直觀接收視覺資訊。
<br>

## 結尾

- UI會依據製作的遊戲類型與方向性而有很大的差異。
- 範例世界的UI是為了展示整體流程而製作，並不一定需要完全照此方式進行。
- 反而積極採用自己的想法並融入其中，能製作出更優秀的遊戲。