# [💡 기초 학습] UI 구성하기
<br>
<br>
<br>

## 영상 타임라인
- 강의 영상 내 아래의 타임라인에서 학습할 수 있습니다.
- UI 구성하기: `01:10:24`

<br>

## 학습 목표
- 게임 내 플레이어에게 현재 점수를 출력하는 방법에 대해 학습합니다.
- 게임의 남은 시간을 출력하는 시간 UI를 출력하는 방법에 대해 학습합니다.
- 게임의 결과를 출력하는 결과 UI를 제작하는 방법에 대해 학습합니다.

<br>

## 해당 영상 시청 후 수행해 볼 내용
- 월드 내에서 플레이어의 정보를 출력하기 위한 UI를 구성합니다.
- 플레이어의 경직 회복까지 남은 시간, 게임 진행 중 UI의 출력 등 다양한 UI를 직접 구성합니다.

<br>

---

## UI 구성하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_001.png" alt="MSW-100_PartyGame_6_001.png">
</p>

- UI는 빠르게 변화하는 월드 환경에서 플레이어에게 정보를 제공하는 역할을 수행합니다.
- 플레이어 캐릭터의 남은 체력, 쿨타임, 적의 HP 등 다양한 정보를 시각적 정보로 제공합니다.
- 좋은 UI는 플레이어가 화면을 볼 때 원하는 정보를 빠르게 확인할 수 있습니다.
- 그러나 UI가 없거나 불필요하게 존재할 경우 난잡하게 보이거나 정보를 얻지 못할 수 있습니다.
- 따라서 UI를 구성하기 위해서는 적절한 위치 선정, 비율 설정, 색채의 대비 등 다양한 요소들을 고려하여 제작해야 합니다.

<br>

### UI 제작 방법

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_002.png" alt="MSW-100_PartyGame_6_002.png">
</p>

- `Hierarchy` 창에서 현재 존재하는 UI Group을 확인할 수 있습니다.
- 하나의 Group이 UI의 사용 목적을 구분하는 용도로 사용됩니다.
- `Hierarchy`기준 가장 **아래**에 있는 uiGroup이 화면의 가장 **최상단**에 출력됩니다.

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_003.png" alt="MSW-100_PartyGame_6_003.png">
</p>

- 메이플스토리 월드 에디터 화면 상단의 UI 버튼을 클릭합니다.
- 화면 하단의 UI Groups 창에서 UI Group을 드래그하여 순서를 변경할 수 있습니다.
- UI Groups 창 내의 `+` 버튼을 클릭하여 새로운 UI Group을 제작할 수 있습니다.
- 쓰레기통 버튼을 클릭하면 현재 선택 중인 UI를 제거할 수 있습니다.
- 샘플 월드에서는 3개의 UI Group을 제작하여 사용하고 있습니다.
    - **CoinRushUI**: 현재 플레이어의 점수를 출력하기 위한 UI입니다.
    - **TimerUI**: CoinRush 미니 게임의 남은 시간과 게임 시작 버튼을 출력하기 위한 UI입니다.
    - **ResultUI**: CoinRush 미니 게임의 결과를 보여주기 위한 UI입니다.

<br>

### UI 엔티티 제작하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_004.png" alt="MSW-100_PartyGame_6_004.png">
</p>

- `Hierarchy`에서 편집 중인 UI Group을 마우스 우클릭합니다.
- `Create Entity`, `Create UIEmpty`를 클릭하여 빈 UI 엔티티를 제작할 수 있습니다.

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_005.png" alt="MSW-100_PartyGame_6_005.png">
</p>

- UIEmpty는 기존의 MapEmpty와 다르게 `TransformComponent`를 가지지 않습니다.
- 대신 `UITransformComponent`를 가지고 있습니다.
- `UITransformComponent`는 다음의 특징을 가지고 있습니다.
    - 화면상 어떤 부분을 중심으로 가질 것 인지 설정하는 **Anchor** 설정이 존재합니다.
    - **Anchor**의 설정에 따라 PosX, PosY, Width, Height의 값을 설정하거나 Left, Right, Center, Bottm 값을 조절하여 엔티티의 위치와 크기를 조절할 수 있습니다.
    - **Pivot**을 활용하여 UI 이미지의 중심을 변경할 수 있습니다.
    - **ActivePlatform**의 값을 활용하여 PC 환경에서만 출력시킬 UI 인지, 모바일 환경에서만 출력해야 하는 UI 인지, 혹은 두 환경 모두 출력해야 하는지 선택할 수 있습니다.

<br>

#### UI 이미지 출력하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_006.png" alt="MSW-100_PartyGame_6_006.png">
</p>

- UI에서 이미지를 출력하기 위해선 `SpriteGUIRendererComponent`를 사용합니다.
- `SpriteRendererComponent`와 같이 ImageRUID에 리소스의 ID 값을 입력하여 이미지를 선택할 수 있습니다.
- ⚠️ `SpriteRendererComponent` VS `SpriteGUIRendererComponent`
    - `SpriteRendererComponent`는 maps 내에 이미지를 출력할 때 사용하는 Component입니다.
    - `SpriteRendererComponent`로 출력하는 이미지의 경우 **캐릭터의 이동**에 따라 화면에서 **출력**이 **중단**될 수 있습니다.
    - `SpriteGUIRendererComponent`의 경우 캐릭터가 이동해도 사라지지 않습니다.
    - `SpriteGUIRendererComponent`는 화면의 크기에 따라 출력되는 위치와 크기가 변경될 수 있습니다.

<br>

#### 텍스트 출력하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_007.png" alt="MSW-100_PartyGame_6_007.png">
</p>

- UI에서 텍스트를 출력해야 하는 경우 `TextGUIRendererComponent`를 활용하여 텍스트를 출력할 수 있습니다.
- 출력하고자 하는 내용을 Text에 입력하여 텍스트를 출력할 수 있습니다.
- FontSize를 통해 텍스트의 크기를 조절할 수 있습니다.
- FontColor를 통해 텍스트의 색상을 변경할 수 있습니다.

<br>

### CoinRushUI 구성하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_008.png" alt="MSW-100_PartyGame_6_008.png">
</p>

- **ScoreFrame**: 점수 판을 출력하기 위해 SprtieGUIRendererComponent를 가지고 있습니다.
- **ScoreText**: 점수 판 위에 현재 점수를 출력하기 위해 TextGUIRendererComponent를 가지고 있습니다.
- CoinRushUI는 자신의 요소들을 관리하기 위해 CoinRushUIController Component를 가지고 있습니다.
- 아래는 CoinRushUIController의 코드입니다.

```lua
@Component
script CoinRushUIController extends Component

property TextGUIRendererComponent scoreText = nil

property number currentScore = 0

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitScoreText()
end

@ExecSpace("ClientOnly")
method void InitScoreText()
-- scoreText가 nil 상태인지 점검합니다.
if not isvalid(self.scoreText) then
	-- 만약 nil 상태일 경우 scoreText를 찾아 등록합니다.
	local findTextEntity = _EntityService:GetEntityByPath("/ui/CoinRushUI/ScoreFrame/ScoreText")
	-- 엔티티를 찾았는지 확인합니다.
	if isvalid(findTextEntity) then
		-- 정상적으로 엔티티를 찾았다면 findTextEntity의 TextGUIRendererComponent를 등록합니다.
		local findTextComponent = findTextEntity.TextGUIRendererComponent
		-- TextGUIRendererComponent를 정상적으로 찾았는지 검증합니다.
		if isvalid(findTextComponent) then
			-- 찾은 findTextComponent를 scoreText에 등록합니다.
			self.scoreText = findTextComponent
		else
			-- 정상적으로 TextGUIRendererComponent를 찾지 못했으므로 에러 로그를 남기고 코드 실행을 중단합니다.
			log_warning("ScoreText의 TextGUIRendererComponent를 찾지 못했습니다!")
			return
		end
	else
		log_warning("ScoreText엔티티를 찾을 수 없습니다!")
		return
	end
end
end

@ExecSpace("ClientOnly")
method void ResetScoreUI()
self.scoreText.Text = "0"
self.currentScore = 0
self.Entity.Enable = true
end

@EventSender("LocalPlayer")
handler HandleGetCoinRushCoin(GetCoinRushCoin event)
self.currentScore = self.currentScore + event.CoinScoreValue
self.scoreText.Text = string.format("Score : %d", self.currentScore)
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

- LocalPlayer가 점수 아이템을 습득할 경우 점수를 UI에서 업데이트하도록 기능이 구성되어 있습니다.

<br>

### TimerUI 구성하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_009.png" alt="MSW-100_PartyGame_6_009.png">
</p>

- **Timer_3**, **Timer_2**, **Timer_1**, **Timer_0**: 미니 게임 시작 전 카운트 다운 연출을 위해 `SpriteGUIRendererComponent`를 가지고 있습니다.
- **StartButton**: 시작 버튼의 프레임을 구성하기 위해 `SpriteGUIRendererComponent`를 가지고 있습니다.
- **StartBUttonText**: 시작 버튼의 이름을 출력하기 위해 `TextGUIRendererComponent`를 가지고 있습니다.
- **TimerFrame**: 남은 시간을 출력하기 위한 프레임을 구성하기 위해 `SpriteGUIRendererComponent`를 가지고 있습니다.
- **TimerText**: 남은 시간을 텍스트로 표기하기 위해 `TextGUIRendererComponent`를 사용하고 있습니다.
- TimerUI는 자신의 요소를 관리하기 위해 TimerUIController Component를 가지고 있습니다.
- 아래는 TimerUIController Component의 코드입니다.

```lua
@Component
script TimerUIController extends Component

property table timerEntity = {}

property string countSoundEffectID = "5dc17871a4e749e093beef9ab1096243"

property string startSoundEffectID = "9925041d48474aeeab6df6e0c4368097"

property Entity startButton = nil

property TextGUIRendererComponent timerText = nil

property Entity timerFrame = nil

@ExecSpace("ClientOnly")
method void OnBeginPlay()
    -- 1부터 4까지 자식 엔티티를 배열에 할당합니다.
    for i = 1, 4 do
        local entityName = "Timer_"..(i - 1)
        local findEntity = self.Entity:GetChildByName(entityName)
        
        if isvalid(findEntity) then
            self.timerEntity[i] = findEntity
            -- 시작 시점에 모든 카운트 이미지 비활성화
            findEntity.Enable = false
        else
            log(entityName .. "을 찾을 수 없습니다. 계층 구조를 확인하세요.")
        end
    end
-- StartButton이 nil 상태일 경우 엔티티를 찾아 등록합니다.
if not isvalid(self.startButton) then
	local findEntity = self.Entity:GetChildByName("StartButton", true)
	if isvalid(findEntity) then
		self.startButton = findEntity
		self.startButton.Enable = false
	end
end
-- timerEntity가 nil일 경우 timerEntity를 찾아 등록합니다.
if not isvalid(self.timerFrame) then
	local findEntity = self.Entity:GetChildByName("TimerFrame", true)
	if isvalid(findEntity) then
		self.timerFrame = findEntity
		self.timerFrame.Enable = false
	end
end


-- timerText가 nil일 경우 timerText를 찾아 등록합니다.
if not isvalid(self.timerText) then
	local findEntity = self.Entity:GetChildByName("TimerText", true)
	if isvalid(findEntity) then
		self.timerText = findEntity.TextGUIRendererComponent
	end
end
end

@ExecSpace("ClientOnly")
method void StartTimer()
self.Entity.Enable = true
local localPlayer = _UserService.LocalPlayer
if not isvalid(localPlayer) then return end

-- 게임 시간 체크용 타이머를 세팅합니다.
_TimerLogic:SetTimer(60)
    
-- 플레이어 이동을 일시적으로 금지시킵니다.
localPlayer.MovementComponent.Enable = false
    
-- 카운트다운을 시작합니다.
for countNumber = 4, 1, -1 do
	local target = self.timerEntity[countNumber]
        
	if isvalid(target) then
		-- 1. 현재 숫자 엔티티를 활성화합니다.
		target.Enable = true
            
		-- 2. 사운드를 재생합니다.
		if countNumber > 1 then
    		_SoundService:PlaySound(self.countSoundEffectID, 1.0)
		else
			_SoundService:PlaySound(self.startSoundEffectID, 1.0)    
		end
            
		-- 3. 1초간 대기합니다.
		wait(1.0)
            
		-- 4. 현재 숫자를 비활성화 합니다.
		if isvalid(target) then
			target.Enable = false
		end
	end
end
    
-- 플레이어 이동을 다시 허용합니다.
localPlayer.MovementComponent.Enable = true
-- 타이머 UI를 활성화합니다.
self.timerFrame.Enable = true
-- 타이머를 활성화합니다.
_TimerLogic:StartTimer()

end

@ExecSpace("ClientOnly")
method void ShowTimerUI()
self.startButton.Enable = true
self.timerFrame.Enable = false
-- 타이머 연출 엔티티를 비활성화 상태로 전환합니다.
for i = 1, #self.timerEntity do
	self.timerEntity[i].Enable = false
end
end

@ExecSpace("ClientOnly")
method void OnUpdate(number delta)
-- 타이머가 실행되고 있지 않을 경우 코드 실행을 중단합니다.
if not _TimerLogic.isRunning then
	return
end

local time = _TimerLogic.remainTime
local min = math.floor(time / 60)
local sec = _TimerLogic.remainTime % 60
self.timerText.Text = string.format("%02d:%02d", min, sec)
end

@ExecSpace("ClientOnly")
@EventSender("Entity", "8708457c-646e-469d-95a5-c10703f14ed4")
handler HandleButtonClickEvent(ButtonClickEvent event)
self.startButton.Enable = false
self:StartTimer()
end

end
```

> 해당 코드는 Plain Text 기준으로 작성된 코드입니다.

- 남은 시간을 계산하는 일을 담당하는 Logic을 샘플 월드에서 사용하고 있습니다.

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_010.png" alt="MSW-100_PartyGame_6_010.png">
</p>

- `Hierarchy`, `MyDesk`를 마우스 우클릭합니다.
- `Create Scripts`, `Create Logic`을 통해 Logic을 제작할 수 있습니다.
- 생성한 Logic의 이름을 TimerLogic으로 선언합니다.
- 아래는 TimerLogic의 코드입니다.

```lua
@Logic
script TimerLogic extends Logic

property number remainTime = 0

property boolean isRunning = false

@ExecSpace("ClientOnly")
method void StartTimer()
-- 타이머의 활성 여부를 활성화로 변경합니다.
self.isRunning = true

-- 현재 맵이 CoinRush인지 검사한 뒤, 현재 맵이 CoinRush일 경우 게임을 실행합니다.
local localPlayer = _UserService.LocalPlayer
if localPlayer.CurrentMapName == "CoinRush" then
	local coinRushController = _EntityService:GetEntityByPath("/maps/CoinRush/CoinRushController")
	coinRushController.CoinRushController:StartGame()
	local coinRushUI = _EntityService:GetEntityByPath("/ui/CoinRushUI")
	coinRushUI.CoinRushUIController:ResetScoreUI()
	
end

-- 루프를 통해 시간을 감소시킵니다.
while self.remainTime > 0 and self.isRunning do
	wait(1.0)
	self.remainTime = self.remainTime - 1
end

-- 시간이 모두 종료되면 결과창을 출력하도록 요청합니다.
if self.isRunning then
	self.isRunning = false
	localPlayer.MovementComponent.Enable = false
	local coinRushController = _EntityService:GetEntityByPath("/maps/CoinRush/CoinRushController")
	coinRushController.CoinRushController:FinishGame()
	local resultUI = _EntityService:GetEntityByPath("/ui/ResultUI")
	resultUI.ResultUIController:ShowResultUI(localPlayer.PlayerCoinRushComponent.playerScore)
end
end

@ExecSpace("ClientOnly")
method void SetTimer(number SetTime)
self.remainTime = SetTime
end

end
```

> 해당 코드는 Plain Text 기준으로 작성된 코드입니다.

<br>

### ResultUI 구성하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_011.png" alt="MSW-100_PartyGame_6_011.png">
</p>

- **NonClickBG**: 결과 UI가 출력되었을 때 결과 UI 뒤로 있는 UI를 터치하는 현상을 막기 위해 SpriteGUIRendererComponent로 제작한 UI입니다.
- **ResultText**: UI의 제목을 작성하기 위해 TextGUIRendererComponent로 제작된 UI입니다.
- **ResultFrame**: 결과 점수를 출력하기 위한 프레임을 위해 SpriteGUIRendererComponent를 사용하고 있습니다.
- **ResultScoreText**: 결과 점수를 출력하기 위해 TextGUIRendererComponent를 사용하고 있습니다.
- **ReturnButton**: 샘플 월드 기준 맵으로 이동하기 위한 버튼의 이미지를 출력하기 위해 SpriteGUIRendererComponent를 사용하고 있습니다.
- **TextComponent**: 돌아가기 버튼의 이름을 출력하기 위해 TextGUIRendererComponent를 사용하고 있습니다.
- ResultUI는 자신의 요소를 관리하기 위해 ResultUIController를 사용하고 있습니다.
- 아래는 ResultUIController의 코드입니다.

```lua
@Component
script ResultUIController extends Component

property TextGUIRendererComponent resultText = nil

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitResultText()
end

@ExecSpace("ClientOnly")
method void InitResultText()
-- resultText가 nil일 경우 resultText를 찾아 등록합니다.
if not isvalid(self.resultText) then
	local findEntity = self.Entity:GetChildByName("ResultScoreText", true)
	if isvalid(findEntity)then
		self.resultText = findEntity.TextGUIRendererComponent
	end
end
end

@ExecSpace("ClientOnly")
method void ShowResultUI(number ResultValue)
local playerProfileID = _UserService.LocalPlayer.PlayerComponent.ProfileCode
if ResultValue > _DataStorageLogic.clientBestScore then
	_DataStorageLogic:RequestSaveScore(playerProfileID, ResultValue)
	self.resultText.Text = _LocalizationService:GetTextFormat("UI_Result", ResultValue, ResultValue)
else
	self.resultText.Text = _LocalizationService:GetTextFormat("UI_Result", ResultValue, _DataStorageLogic.clientBestScore)
end
self.Entity.Enable = true
end

@ExecSpace("ClientOnly")
@EventSender("Entity", "03be7e6d-2983-4972-a95e-10bad9a57892")
handler HandleButtonClickEvent(ButtonClickEvent event)
-- 플레이어 엔티티를 가져옵니다.
local findLocalPlayer = _UserService.LocalPlayer
-- TimerUI를 가져온 뒤 UI를 비활성화합니다.
local timerUI = _EntityService:GetEntityByPath("/ui/TimerUI")
timerUI.Enable = false
-- CoinRushUI를 가져온 뒤 UI를 비활성화합니다.
local coinRushUI = _EntityService:GetEntityByPath("/ui/CoinRushUI")
coinRushUI.CoinRushUIController:ResetScoreUI()
coinRushUI.Enable = false
-- 플레이어를 게임 선택 맵으로 이동시킵니다.
findLocalPlayer.MovementComponent.Enable = true
findLocalPlayer.PlayerTeleportComponent:RequestPlayerChoiceMap(findLocalPlayer)
self.Entity.Enable = false
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

- 또한 플레이어가 이동하기 위해 `DefaultPlayer`에 `PlayerTeleportComponent`를 추가하여 사용하고 있습니다.
- 아래는 PlayerTeleportComponent의 코드입니다.

```lua
@Component
script PlayerTeleportComponent extends Component

@ExecSpace("Server")
method void RequestPlayerChoiceMap(Entity PlayerEntity)
self:TeleportChoiceMap(PlayerEntity)
end

@ExecSpace("ServerOnly")
method void TeleportChoiceMap(Entity PlayerEntity)
_TeleportService:TeleportToEntityPath(PlayerEntity, "/maps/ChoiceMap")
end

@EventSender("Self")
handler HandleEntityMapChangedEvent(EntityMapChangedEvent event)
if event.NewMap.Name == "ChoiceMap" then
	_SoundService:PlayBGM("8b57ad3bb40742a1b4955390bf14b36d", 1.0)
else
	
end
end

end
```

### 맵 이동시 UI 출력하기

<p align="center">
<img src="../../_images/MSW-100_PartyGame_6_012.png" alt="MSW-100_PartyGame_6_012.png">
</p>

- 플레이어가 샘플 월드의 포탈을 이용하면 이를 감지하여 UI를 업데이트하도록 포탈 엔티티에 InteractionPortal Component를 제작하여 사용하고 있습니다.
- 아래는 INteractionPortal 코드입니다.

```lua
@Component
script InteractionPortal extends Component

@EventSender("Self")
handler HandlePortalUseEvent(PortalUseEvent event)
-- 포탈을 사용할 경우 TimerUI를 활성화시킵니다.
if event.PortalUser == _UserService.LocalPlayer then
	local timerUI = _EntityService:GetEntityByPath("/ui/TimerUI")
	event.PortalUser.PlayerCoinRushComponent:ResetScore()
	timerUI.Enable = true
	timerUI.Visible = true
	timerUI.TimerUIController:ShowTimerUI()
	-- 배경음을 변경합니다.
	_SoundService:PlayBGM("ae1e81ea94f5421580ecbdf1b0fddfaf", 1.0)
end

end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

<br>

## 최소 구현 기준
- 미니 게임에서 남은 시간을 UI로 표시합니다.
- 게임 시작 UI와 결과 UI를 통해 플레이어가 게임 시작, 종료를 확인할 수 있도록 UI를 구성합니다.

<br>

## 응용 및 확장 방향

- Tween 기능을 활용하여 UI의 연출을 구성합니다.
- UI의 구성 이미지를 직접 다른 리소스로 교체하여 시각적 정보를 효과적으로 전달하게 구성합니다.