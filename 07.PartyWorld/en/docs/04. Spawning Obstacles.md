# [💡 Basic Training] Spawning Obstacles
<br>
<br>
<br>

## Video Timeline
- You can follow along by using the timeline under the training video.
- Spawning Obstacles: `00:51:09`

<br>

## Learning Goals
- Learn how to spawn obstacles at regular intervals and how to use them.
- Learn how to create the Component that's in charge of obstacle creation.

<br>

## Practical Exercises to Do After Watching the Video
- Directly create and spawn a Controller that generates obstacles.
- Modify the function so that the obstacle creation interval can be adjusted and operate at the intended interval.
- Edit the function so that various obstacles can be generated.

<br>

---

## Creating a Controller

<p align="center">
<img src="../../_images/MSW-100_PartyGame_4_001.png" alt="MSW-100_PartyGame_4_001.png">
</p>

- You can see an entity named CoinRushController that exists inside of the sample world's CoinRush map.
- CoinRushController is the Component that manages the processing needed for the CoinRush mini game.
- In charge of spawning obstacles and scoring items.

<br>

### Creating a CoinRushController
- Create an empty entity.
- Assign the TransformComponent value to the center of the map.
- Create a CoinRushController as a new Component.
- Write the following code to the CoinRushController.

```lua
@Component
script CoinRushController extends Component

property number logCallTimer = 5.0

property number specialLogTime = 3

property number specialLogTimeCount = 0

property integer logTimerID = 0

property table logTable = {}

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitLog()
end

@ExecSpace("ClientOnly")
method void InitLog()
-- Executes the loop a number of times equal to the number of wooden logs.
for i = 1, 3 do
	-- Calls the path in accordance to each wooden log's name.
	local entityName = "/maps/CoinRush/Log_"..i
	-- Searches for the wooden log entity based on the path.
	local findEntity = _EntityService:GetEntityByPath(entityName)
	if isvalid(findEntity) then
		self.logTable[i] = findEntity
	end
end
end

@ExecSpace("ClientOnly")
method void StartLogLogic()
-- Attempts a reset if logTimerID isn't 0.
if self.logTimerID ~= 0 then
	_TimerService:ClearTimer(self.logTimerID)
	self.logTimerID = 0
end
-- Uses TimerService, so a wooden log can spawn.
self.logTimerID = _TimerService:SetTimerRepeat(function()
	self:SpawnLog()
end, self.logCallTimer, self.logCallTimer)
end

@ExecSpace("ClientOnly")
method void SpawnLog()
if self.specialLogTimeCount >= self.specialLogTime then
	-- Sets 1 wooden log.
	local firstLogPos = _UtilLogic:RandomIntegerRange(1, 3)
	-- Draws 1 wooden log with a different number from the one that was drawn first.
	local secondLogPos = 0
	repeat
		secondLogPos = _UtilLogic:RandomIntegerRange(1, 3)
	until secondLogPos ~= firstLogPos
	-- Plays after activating the selected 2 wooden logs.
	self.logTable[firstLogPos].Enable = true
	self.logTable[secondLogPos].Enable = true
	self.logTable[firstLogPos].TweenLineComponent:Stop(true)
	self.logTable[firstLogPos].TweenLineComponent:Play()
	self.logTable[secondLogPos].TweenLineComponent:Stop(true)
	self.logTable[secondLogPos].TweenLineComponent:Play()
	-- Sets a Timer so that the wooden log automatically deactivates after a set time.
	_TimerService:SetTimerOnce(function()
		self.logTable[firstLogPos].TweenLineComponent:Stop(true)
		self.logTable[secondLogPos].TweenLineComponent:Stop(true)
		self.logTable[firstLogPos].Enable = false
		self.logTable[secondLogPos].Enable = false
	end, self.logTable[firstLogPos].TweenLineComponent.Duration)
	self.specialLogTimeCount = 0
else
	-- Sets 1 wooden log.
	local firstLogPos = _UtilLogic:RandomIntegerRange(1, 3)
	-- Plays the selected wooden log.
	self.logTable[firstLogPos].Enable = true
	self.logTable[firstLogPos].TweenLineComponent:Stop(true)
	self.logTable[firstLogPos].TweenLineComponent:Play()
	-- Increases the special wooden log count by 1.
	self.specialLogTimeCount = self.specialLogTimeCount + 1
	-- Sets a Timer so that the wooden log automatically deactivates after a set time.
	_TimerService:SetTimerOnce(function()
		self.logTable[firstLogPos].TweenLineComponent:Stop(true)
		self.logTable[firstLogPos].Enable = false
	end, self.logTable[firstLogPos].TweenLineComponent.Duration)
end
end

@ExecSpace("ClientOnly")
method void StartGame()
self:StartLogLogic()
end

@ExecSpace("ClientOnly")
method void FinishGame()
-- Deactivates all wooden log models.
for i = 1, #self.logTable do
	self.logTable[i].TweenLineComponent:Stop(true)
	self.logTable[i].Enable = false
end
-- Removes all reserved timer events.
_TimerService:ClearTimer(self.logTimerID)
end

end
```

> This code is written based on Plain Text.

- This code is why the names have been consistently designated as Log_1, Log_2, and Log_3 in the sample world.
- This locates the entity based on the for statement's i when using and registering multiple obstacle entities.

<br>

## Placing Obstacle Entities

<p align="center">
<img src="../../_images/MSW-100_PartyGame_4_002.png" alt="MSW-100_PartyGame_4_002.png">
</p>

- Copy the existing obstacle entities and designate their names as Log_1, Log_2, and Log_3.
- Assign each of their positions to appropriate locations.
- If you finished placing the entities, switch the Enable value to false via the `Property` window.
- Doing this will finalize the composition of the function that allows evasion in a very narrow area when a special obstacle is created.

<br>

## The Mechanism
- Searches for the Log_1, Log_2, and Log_3 entities via OnBeginPlay and registers them to Property.
- CoinRushController calls StartLogLogic Method if StartGame Method is called.
- StartLogLogic executes the function repeatedly via TimerService so that obstacles will spawn at specified intervals.
- FinishGame removes the reserved TimerService if the game is over.

<br>

## Minimum Implementation Standards
- Obstacles must be executed at regular intervals via the execution Method.
- The function must be designed to use Timer to ensure spawns happen at designated times.

<br>

## Usage and Extension Direction
- Modify the function to ensure obstacles spawn in a special pattern.
- Modify the function so that obstacles of varied designs are displayed instead of obstacles of singular design.
- Design the function to ensure each obstacle has its own adjustable speed, thereby producing obstacles with varied speeds.
- Design the function to ensure that the obstacle spawn time changes at designated times.