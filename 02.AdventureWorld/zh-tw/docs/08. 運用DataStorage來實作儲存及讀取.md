# [📖進階學習] 運用DataStorage來實作儲存及讀取
<br>
<br>
<br>

## 影片時間軸
- 可在課程影片中以下時間點進行學習。
- 運用DataStorage：`01:32:52`

<br>

## 學習目標
- 運用DataStorage，實作儲存玩家遊玩資訊的功能。
- 運用DataStorage，實作讀取玩家遊玩資訊的功能。
- 讀取玩家資訊後，依照讀取的值，實作使遊戲能夠繼續進行的功能。

<br>

## 觀看該影片後可嘗試執行的內容
- 組成Title畫面並製作讀取功能。
- 配置執行儲存功能的實體，並製作能夠執行儲存功能的機制。
- 擴充任務功能，檢查玩家任務資訊是否正常儲存及讀取。

<br>

## 實作儲存及讀取

<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Adventure/MSW-100_Adventure_8_001.png" alt="MSW-100_Adventure_8_001.png">
</p>

> 此為使用Gemini產生的AI圖片。

- 如果玩家很努力玩遊戲，成果卻沒有留下，那麼玩家就會感覺到挫折。
- 為了解決這個問題，必須製作功能，以實作能夠儲存及讀取玩家資訊的功能。
- 為了實作儲存及讀取的功能，在楓之谷世界中提供DataStorage這個API。

<br>

## DataStorageService
- 「DataStorageService」提供各式各樣的DataStorage存取功能。
- 範例世界中主要使用的功能是「GetUserDataStorage」、「GetAndWait」和「SetAndWait」。
    - 「GetUserDataStorage」：取得特定使用者專用UserDataStorage的功能。
    -「GetAndWait」：從透過「GetUserDataStorage」取得的資訊中，取得特定Key值。
    -「SetAndWait」：以「GetUserDataStorage」中的key為基準儲存數值。
- 如果想確認更多功能，可以點擊楓之谷世界編輯器上方的「Help」、「API Reference」之後，在該頁面中搜尋「DataStorageService」，即可確認更多功能。

<br>

## 製作DataStorageLogic
- 在「Workspace」中點擊「MyDesk」。
- 透過「Create Scripts」、「Create Logic」來建立Logic。
- 將建立的Logic名稱變更為「DataStorageLogic」。

```lua
@Logic
script DataStorageLogic extends Logic

@Sync
property string lastMapName = ""

@Sync
property Vector3 lastPosition = Vector3.zero

property string localUserID = ""

property table userInventory = {}

@Sync
property string questID = ""

@ExecSpace("ClientOnly")
method void OnBeginPlay()
-- 儲存本機玩家的使用者ID。
self.localUserID = _UserService.LocalPlayer.PlayerComponent.UserId
-- 將本機玩家的使用者ID傳遞至伺服器腳本並記錄。
self:SendUserIDToServer(self.localUserID)
end

@ExecSpace("Server")
method void SavePlayerData()
-- 在伺服器中取得使用者資料儲存空間。
local userDataStorage = _DataStorageService:GetUserDataStorage(self.localUserID)
-- 儲存目前玩家所在的地圖名稱。
userDataStorage:SetAndWait("lastMapName", self.lastMapName)
-- 儲存目前玩家所在位置。
userDataStorage:SetAndWait("lastPosition", tostring(self.lastPosition))
-- 儲存目前玩家正在執行的任務ID。
userDataStorage:SetAndWait("QuestID", self.questID)
-- 以JSON形式儲存目前使用者的背包狀態。
local jsonString = _HttpService:JSONEncode(self.userInventory)
userDataStorage:SetAndWait("InventoryData", jsonString)
log("背包儲存完成")
end

@ExecSpace("Server")
method void LoadPlayerData()
-- 在伺服器中取得使用者的資料儲存空間。
local userDataStorage = _DataStorageService:GetUserDataStorage(self.localUserID)
-- 如果取得的資料儲存空間為空，則中斷程式碼執行。
if userDataStorage == nil then 
	return
end
-- 在伺服器中取得lastMapName值。
local errorCode, mapName = userDataStorage:GetAndWait("lastMapName")
-- 取得儲存最後位置的lastPosition值。
local errorCodePos, posString = userDataStorage:GetAndWait("lastPosition")
-- 取得使用者的背包資訊。
local itemErrorCode, jsonData = userDataStorage:GetAndWait("InventoryData")
-- 取得使用者執行中的任務資訊。
local questErrorCode, questData = userDataStorage:GetAndWait("QuestID")

-- 讀取各項資料時，都必須確認錯誤代碼是否為0(成功)。
-- 登錄最後地圖位置。
if errorCode == 0 then
	if _UtilLogic:IsNilorEmptyString(mapName) then
		return
	else
		self.lastMapName = mapName
	end
else
	log("無法讀取lastMapName！")
	return
end
-- 取得角色最後位置之後，將該位置進行登錄。
if errorCodePos == 0 then
	self.lastPosition = Vector3(0, 0, 0)
	local clean_text, arr = string.gsub(posString, "[%(%)]", "")
	local coords = _UtilLogic:Split(clean_text, ",")
	if #coords == 3 then
		self.lastPosition.x = tonumber(coords[1])
		self.lastPosition.y = tonumber(coords[2])
		self.lastPosition.z = tonumber(coords[3])
	end
else
	log("無法讀取lastPosition！")
end
-- 讀取背包資料之後，登錄背包資料。
if itemErrorCode == 0 then
	-- 如果沒有讀取的背包資料，則留下log。
	if _UtilLogic:IsNilorEmptyString(jsonData) then
		log("沒有讀取道具資料！")
	else
		-- 如果存在正常數值，則登錄道具資料。
		local itemData = _HttpService:JSONDecode(jsonData)
    	log("讀取的道具ID：" ..itemData[1].ItemID)
		self.userInventory = itemData
		log("背包讀取完成")
		self:UpdateClientUserInventory(self.userInventory)
	end    
else
	log("背包資料讀取失敗。")
end
-- 登錄任務資料。
if questErrorCode == 0 then
	self.questID = questData
	self:UpdateClientQuestData(questData)
else
	log("從伺服器讀取QuestData失敗。")
end

log("資料讀取完成：" ..self.lastMapName .." / " ..tostring(self.lastPosition))
log("InventoryData讀取完成")
-- 由於資料登錄完成，因此重置背包及任務資訊視窗。
self:SetUserInventoryData()
self:SetClientQuestData()
return
end

@ExecSpace("Client")
method void RequestSaveData()
-- 找到本機玩家實體後，預先取得必須資料。
local userEntity = _UserService.LocalPlayer
local userItemData = userEntity.PlayerItemComponent.playerInventory
self.userInventory = userItemData
local questID = userEntity.PlayerQuestComponent.currentQuestData
-- 在執行儲存至伺服器之前，預先更新要儲存至伺服器的值。
self:UpdateServerItemData(userItemData)
self:UpdateServerMapData(userEntity.CurrentMapName, userEntity.TransformComponent.Position)
self:UpdateServerQuestData(questID.QuestID)
-- 向伺服器請求儲存資訊。
self:SavePlayerData()
end

@ExecSpace("Client")
method void SendUserIDToServer(string UserID)
-- 用來傳遞本機玩家ID值至伺服器的Method。
self:UpdateServerUserID(UserID)
end

@ExecSpace("Server")
method void UpdateServerUserID(string UserID)
-- 在伺服器中執行，並將本機玩家的ID值登錄至伺服器。
self.localUserID = UserID
end

@ExecSpace("Server")
method void UpdateServerMapData(string CurrentMapName, Vector3 CurrentPosition)
-- 將角色所在的地圖名稱及位置登錄至伺服器。
self.lastMapName = CurrentMapName
self.lastPosition = CurrentPosition
end

@ExecSpace("Server")
method void UpdateServerItemData(table ItemData)
-- 將目前玩家的背包資訊登錄至伺服器。
self.userInventory = ItemData
end

@ExecSpace("Client")
method void SetUserInventoryData()
-- 在伺服器中逐一將道具登錄至背包時，執行Method使背包能夠更新。
local localUser = _UserService.LocalPlayer
for i = 1, #self.userInventory do
	local addItemEvent = OnPlayerLootItem()
	addItemEvent.ItemID = self.userInventory[i].ItemID
	addItemEvent.ItemCount = self.userInventory[i].ItemCount
	localUser:SendEvent(addItemEvent)
end
end

@ExecSpace("Client")
method void UpdateClientUserInventory(table UserInventory)
-- 請求Method將玩家背包資訊更新至伺服器。
self.userInventory = UserInventory
end

@ExecSpace("Server")
method void UpdateServerQuestData(string QuestID)
-- 將目前QuestID登錄至伺服器。
self.questID = QuestID
end

@ExecSpace("Client")
method void SetClientQuestData()
-- 呼叫Logic，使UI根據目前進行中的Quest進行更新。
_QuestLogic:SetQuestDataByQuestID(self.questID)
end

@ExecSpace("Client")
method void UpdateClientQuestData(string QuestID)
-- 請求將目前QuestID登錄至伺服器。
self.questID = QuestID
end

@ExecSpace("Client")
method string RequestLoadData()
if not (_UtilLogic:IsNilorEmptyString(self.lastMapName)) then
	return self.lastMapName
else
	-- 請求讀取玩家資訊。
	self:LoadPlayerData()
	wait(1)
	return self.lastMapName
end
end

end
```

> 該程式碼是以Plain Text為基準編寫而成。

- 在該Logic中，由「SavePlayerData」和「LoadPlayerData」負責執行將玩家資訊儲存至伺服器，以及從伺服器讀取玩家資訊的功能。
- 由於在範例世界中是以Client端為基礎來運作，因此由Client透過「RequestSaveData」和「RequestLoadData」向Server請求執行功能。

<br>

## 範例世界的問題

<br>

### 理想的Server及Client架構
<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Adventure/MSW-100_Adventure_8_002_tw.png" alt="MSW-100_Adventure_8_002_tw.png">
</p>

- 當Client請求演算時，則由Server進行演算。
- Client會根據Server傳遞的值來更新。
- 由於該方式是在Server進行所有值的演算，因此也會由Server進行更新。

### ⚠️ 範例世界的架構
<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Adventure/MSW-100_Adventure_8_003_tw.png" alt="MSW-100_Adventure_8_003_tw.png">
</p>

- 在範例世界中，功能是以Client為基礎進行運作。
- 因此不會對Server請求處理任何值，或傳遞值的變更。
- 由於這種架構，因此Server不具備遊戲的任何值，也不會進行演算。
- 而由於Server不具備任何值，因此如果要儲存的時候，就會發生只能儲存空值的問題。

### ⚠️ 範例世界的解決方式
<p align="center">
<img src="https://develrocket-bucket.s3.ap-northeast-2.amazonaws.com/learning/MSW-100/Adventure/MSW-100_Adventure_8_004_tw.png" alt="MSW-100_Adventure_8_004_tw.png">
</p>

- 範例世界為了解決這個問題，因此目前會強制將值傳遞至Server。
- 在「DataStorageLogic」中會由以下Method執行此功能。
    - `UpdateServerUserID`
    - `UpdateServerMapData`
    - `UpdateServerItemData`
    - `UpdateClientUserInventory`
    - `UpdateClientQuestData`
- 執行這些Method之後，將以請求將傳遞至Server的值進行儲存的形式來運作。

<br>

### ⚠️ 問題和解決方案
- 問題
    - 由於Server接收未驗證的值並直接使用，因此容易受到資料竄改的影響。
    - 這個問題是推動商業化時的最大阻礙。
    - 此外，由於不是由Server進行演算，因此資料的防護也較脆弱。
- 解決方案
    - 將玩家的任務資訊、道具資訊等各種資訊由Server進行演算的功能。
    - Client只根據從Server接收的Data執行UI的更新。

<br>

## 最低實作標準
- 必須儲存玩家資訊。
- 執行讀取時，實作讀取玩家資訊之後，根據玩家資訊繼續進行遊戲的功能。


## 應用與擴展方向
- 運用Server和Client之間的同步功能，將數值重構改為由Server進行更新。
- 依照同步功能，使儲存Method的處理方式優化。