# [💡 기초 학습] 적 구현하기
<br>
<br>
<br>

## 영상 타임라인
- 강의 영상내 아래의 타임라인에서 학습할 수 있습니다.
- 적 구현하기: `00:12:54`

<br>

## 학습 목표
- Preset List의 몬스터를 활용하여 적을 구현하는 방법에 대해 학습합니다.
- 적이 공격을 받을 수 있도록 기능을 구성하는 방법에 대해 학습합니다.
- 몬스터 사망시 처리를 수행할 수 있도록 기능을 구성하는 방법에 대해 학습합니다.

<br>

## 해당 영상 시청 후 수행해 볼 내용
- 원하는 디자인의 몬스터와 전투를 수행할 수 있도록 몬스터를 제작합니다.
- 몬스터와 플레이어가 서로에게 공격을 수행할 수 있는 기능을 제작합니다.

<br>

## 몬스터 제작하기
<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_001.png" alt="MSW-100_Adventure_Extend_2_001.png">
</p>

- 어드벤처 월드에서 플레이어는 다양한 형태의 위험을 경험하게 됩니다.
- 몬스터는 플레이어에게 직접적인 위험 요인이지만 이를 극복하고 성장하게 되는 요소이기도 합니다.

<br>

### 몬스터 엔티티 구성하기
<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_002.png" alt="MSW-100_Adventure_Extend_2_002.png">
</p>

- 몬스터를 구성하는 방법은 여러 방법이 있습니다.
- 직접 엔티티를 생성하고, 필요한 Component를 부여하여 몬스터를 구성할 수 있습니다.
- 샘플 월드에서는 **PresetList**에 제작되어 있는 몬스터를 활용하여 제작했습니다.
- **PresetList**에서 원하는 몬스터를 선택한 뒤 드래그 & 드롭을 통해 몬스터를 **Scene**에 배치할 수 있습니다.

<br>

### 몬스터의 공격 & 피격 준비하기
<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_003.png" alt="MSW-100_Adventure_Extend_2_003.png">
</p>

- **Preset List**로 제작된 몬스터에겐 **HitComponent**가 존재합니다.
- **Hit Component**는 엔티티가 피격할 수 있도록 제작된 Component입니다.
- **Hit Component**의 **CollisionGroup**을 Monster로 변경하여 해당 엔티티의 피격 그룹을 Monster로 설정합니다.
- **TriggerComponent**를 추가하여 플레이어가 몬스터와 충돌 할 경우 플레이어가 데미지를 입도록 준비합니다.
- **TriggerComponent**의 BoxSize, ColliderOffset을 조절하여 몬스터의 공격 범위를 지정합니다.
- **CollisionGroups**을 Monster로 설정하여 공격하는 주체가 Monster임을 설정합니다.

<br>

#### CollisionGroup 변경하기
<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_004.png" alt="MSW-100_Adventure_Extend_2_004.png">
</p>

- **CollisionGroups**을 설정하는 항목에서 우측의 확장 메뉴를 클릭하여 **CollisionGroups**을 관리할 수 있습니다.
- 다른 방법으로는 메이플스토리 월드 에디터 상단의 **Panels** - **Collision Groups**을 통해 관리할 수 있습니다.

<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_005.png" alt="MSW-100_Adventure_Extend_2_005.png">
</p>

- **Add Collision Group**을 통해 새로운 충돌 그룹을 생성할 수 있습니다.
- 생성된 그룹의 이름을 **Player**, **Monster**와 같이 등록하여 충돌 그룹을 지정할 수 있습니다.

<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_006.png" alt="MSW-100_Adventure_Extend_2_006.png">
</p>

- **Matrix** 탭을 통해 각 **Collision Groups**간의 충돌 검사 여부를 설정할 수 있습니다.
- 충돌 관계를 설정하기 위해 Matrix를 보는 방법은 다음과 같습니다.
    - **1번**과 **2번** 그룹은 서로 충돌해야 하는가?
    - 충돌을 해야 하는 경우 체크박스를 설정합니다.
    - 만약 서로 충돌하지 않는 경우 체크박스를 해제합니다.

<br>

### 몬스터의 스탯 구현하기
<p align="center">
<img src="../../_images/MSW-100_Adventure_Extend_2_007.png" alt="MSW-100_Adventure_Extend_2_007.png">
</p>

- 몬스터 역시 공격력, 방어력, 체력과 같은 값들을 보유하고 있습니다.
- 샘플 월드에서는 이런 몬스터의 값들을 관리하기 위해 **MonsterData**를 사용하고 있습니다.

<table class="tg"><thead>
  <tr>
    <th class="tg-baqh">이름</th>
    <th class="tg-baqh">설명</th>
  </tr></thead>
<tbody>
  <tr>
    <td class="tg-0lax">MonsterID</td>
    <td class="tg-0lax">몬스터를 구분하기 위한 ID 값입니다.</td>
  </tr>
  <tr>
    <td class="tg-0lax">MaxHP</td>
    <td class="tg-0lax">몬스터의 초기 체력을 설정하기 위한 값입니다.</td>
  </tr>
  <tr>
    <td class="tg-0lax">Def</td>
    <td class="tg-0lax">몬스터의 방어력을 설정하기 위한 값입니다.</td>
  </tr>
  <tr>
    <td class="tg-0lax">CriticalRate</td>
    <td class="tg-0lax">몬스터의 크리티컬 확률을 설정하기 위한 값입니다.</td>
  </tr>
  <tr>
    <td class="tg-0lax">CriticalRes</td>
    <td class="tg-0lax">몬스터의 크리티컬 공격 저항력을 설정하기 위한 값입니다.</td>
  </tr>
  <tr>
    <td class="tg-0lax">CharacterAtkValue</td>
    <td class="tg-0lax">몬스터의 공격력을 설정하기 위한 값입니다.</td>
  </tr>
</tbody>
</table>

- 이어서 몬스터의 정보를 가져오기 위해 **MonsterData** DataSet을 관리하는 Logic을 제작합니다.
- 샘플 월드에서는 **MonsterDataLogic**이라는 Logic을 사용하고 있습니다.
- 아래는 **MonsterDataLogic**의 코드입니다.
```lua
@Logic
script MonsterDataLogic extends Logic

property table monsterData = {}

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitMonsterData()
end

@ExecSpace("ClientOnly")
method void InitMonsterData()
-- monsterData가 비어있는 상태인지 점검합니다.
if not isvalid(self.monsterData) or not next(self.monsterData) then
	-- 비어있는 상태일 경우 DataSet을 불러옵니다.
	local getData = _DataService:GetTable("MonsterData")
	-- DataSet이 정상적으로 존재하는지 점검합니다.
	if not isvalid(getData) then
		-- 만약 존재하지 않을 경우 에러 로그를 남깁니다.
		log_error("MonsterData가 존재하지 않습니다!")
		return
	end
	for i = 1, getData:GetRowCount() do
		local rowData = StatData()
		local monsterID = getData:GetCell(i, "MonsterID")
		rowData.MaxHP = tonumber(getData:GetCell(i, "MaxHP"))
		rowData.Def = tonumber(getData:GetCell(i, "Def"))
		rowData.CharacterAtkValue = tonumber(getData:GetCell(i, "CharacterAtkValue"))
		rowData.CriticalRate = tonumber(getData:GetCell(i, "CriticalRate"))
		rowData.CriticalRes = tonumber(getData:GetCell(i, "CriticalRes"))
		self.monsterData[monsterID] = rowData
	end
end
end

@ExecSpace("ClientOnly")
method StatData GetMonsterDataByMonsterID(string MonsterID)
if isvalid(self.monsterData[MonsterID]) then
	return self.monsterData[MonsterID]
else
	log_error(MonsterID.."가 존재하지 않는 MonsterData입니다!")
	return nil
end
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

- 마지막으로 몬스터가 자신의 스탯을 등록하고 사용할 수 있도록 몬스터 엔티티에게 Component를 부여해야 합니다.
- 샘플 월드에서는 해당 기능을 수행하기 위해 **MonsterStatComponent**를 제작하여 사용하고 있습니다.
- 아래는 **MonsterStatComponent**의 코드입니다.

```lua
@Component
script MonsterStatComponent extends Component

property StatData monsterStat = StatData()

property integer currentHp = 100

property integer maxHp = 100

property any animEndHandler = nil

property boolean isDead = false

property string monsterID = ""

@ExecSpace("ClientOnly")
method void OnBeginPlay()
self:InitMonsterData()
end

method Entity GetSpriteEntity()
-- 저장된 spriteEntity를 반환합니다. 없으면 루트 엔티티를 반환합니다.
if self._T.spriteEntity ~= nil and isvalid(self._T.spriteEntity) then
	return self._T.spriteEntity
end
return self.Entity
end

method void InitMonsterData()
-- monsterID가 nil 상태인지 점검합니다.
if _UtilLogic:IsNilorEmptyString(self.monsterID) then
	log("monsterID가 nil입니다!")
	return
end
-- MonsterDataLogic에서 정보를 가져옵니다.
local getData = _MonsterDataLogic:GetMonsterDataByMonsterID(self.monsterID)
-- 정상적으로 값을 가져왔다면 정보를 갱신합니다.
if isvalid(getData) then
	self.monsterStat = getData
	self.currentHp = self.monsterStat.MaxHP
end

self.Entity.StateComponent:ChangeState("IDLE")
end

@ExecSpace("ClientOnly")
@EventSender("Self")
handler HandleHitEvent(HitEvent event)
-- 피격 데미지를 처리합니다.
local damage = event.TotalDamage
self.currentHp = self.currentHp - damage
log("MonsterStatComponent: [" .. self.Entity.Name .. "] 피격 - 데미지: " .. tostring(damage) .. " / 잔여 HP: " .. tostring(self.currentHp))

local stateComp = self.Entity.StateComponent

-- HP가 0 이하인 경우 사망 처리를 수행합니다.
if self.currentHp <= 0 then
	self.currentHp = 0
	self.isDead = true
	log("MonsterStatComponent: [" .. self.Entity.Name .. "] 사망 - DEAD 상태로 전환")
	if isvalid(stateComp) then
		stateComp:ChangeState("DEAD")
	end
	return
end

-- 생존 시 HIT 상태로 전환합니다.
if isvalid(stateComp) then
	stateComp:ChangeState("HIT")
end
end

@EventSender("Self")
handler HandleSpriteAnimPlayerEndFrameEvent(SpriteAnimPlayerEndFrameEvent event)
-- 엔티티가 활성화 되어 있지 않을 경우 코드 실행을 중단합니다.
if not isvalid(self.Entity) then return end
-- 현재 상태를 가져오기 위해 StateComponent를 가져옵니다.
local stateComponent = self.Entity.StateComponent
-- 현재 State가 DEAD일 경우, 사망 애니메이션의 마지막이 재생되었으므로 엔티티를 비활성화합니다.
if stateComponent.CurrentStateName == "DEAD" then
	self.Entity:SetEnable(false)
end
end

end
```

> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

- 마지막으로 몬스터가 플레이어를 공격할 수 있도록 기능을 구성해야 합니다.
- **AttackComponent**를 Extend하여 **MonsterAttackComponent**를 제작합니다.
- 아래는 **MonsterAttackComponent*의 코드입니다.

```lua
@Component
script MonsterAttackComponent extends AttackComponent

method boolean CalcCritical(Entity attacker, Entity defender, string attackInfo)
-- 크리티컬 확률을 계산하여 크리티컬 여부를 반환합니다.
---@type MonsterStatComponent
local attackerStat = attacker:GetComponent("script.MonsterStatComponent")
---@type PlayerStatComponent
local defenderStat = defender:GetComponent("script.PlayerStatComponent")
if not isvalid(attackerStat) or not isvalid(defenderStat) then return false end
local criRate = _UtilLogic:RandomIntegerRange(1, 100)
local calcValue = attackerStat.monsterStat.CriticalRate - defenderStat.playerStat.CriticalRes
if calcValue <= 0 then
	return false
elseif calcValue >= criRate then
	return true
else
	return false
end
end

method integer CalcDamage(Entity attacker, Entity defender, string attackInfo)
return _DamageCalcLogic:CalculateMonsterAttack(self.Entity, defender)
end

@ExecSpace("ClientOnly")
@EventSender("Self")
handler HandleTriggerEnterEvent(TriggerEnterEvent event)
-- 만약 몬스터가 사망 상태일 경우 실행을 중단합니다.
if self.Entity.MonsterStatComponent.isDead then
	return
end
-- 충돌한 엔티티(대상)를 변수에 담습니다.
local targetEntity = event.TriggerBodyEntity
-- 대상 엔티티 자체가 유효한지(파괴되지 않았는지) 먼저 검사합니다. (가장 중요)
if isvalid(targetEntity) and targetEntity == _UserService.LocalPlayer then
	-- 정상적으로 Component가 존재한다면 데미지 전달을 준비합니다.
	local isCritical = self:CalcCritical(self.Entity, targetEntity, "")
	local getDamage = self:CalcDamage(self.Entity, targetEntity,"")
	-- HitEvent에 필요한 요소를 모두 담았다면 PlayerEntity에 이벤트를 전달합니다.
	_UserService.LocalPlayer.HitComponent:OnHit(self.Entity, getDamage, isCritical, "", 1)
end
end

end
```
> 해당 코드는 Plain Text를 기준으로 작성된 코드입니다.

<br>

## 몬스터의 공격 방식 학습하기
- 샘플 월드에서는 몬스터와 플레이어의 충돌 **순간*을 검사합니다.
- 따라서 1회성 데미지를 입기 때문에 지속적인 공격이 아닙니다.
- 그러나 플레이어의 공격은 공격을 수행할 때 마다 데미지 계산이 이뤄집니다.
- 따라서 데미지 계산의 방식을 단순 **TriggerComponent**의 **TriggerEnterEvent**에 의존하는 방식이 아닌 다른 방법들을 고안해야 합니다.
    - EX: 플레이어가 공격 범위에 들어 올 경우 일정 주기마다 공격을 수행하게 만들기.
    - EX2: 공격 범위에 플레이어가 들어 올 경우, Attack을 활용하여 공격 수행하게 만들기.
    - 플레이어가 공격 범위를 벗어 날 경우 공격 대상을 nil로 변경하여 허공에 공격을 반복하는 행위 방지하기.

<br>

## 최소 구현 기준
- 몬스터가 본인 고유의 스탯을 가지고, 사용할 수 있어야 합니다.
- 플레이어와 몬스터가 서로 공격을 수행하고, 데미지 계산이 이뤄져야 합니다.

<br>

## 응용 및 확장 방향
- 몬스터의 공격 방식을 지속적인 공격도 수행할 수 있도록 기능을 변경합니다.
- 플레이어와 몬스터의 공격이 이뤄질 때, 피격시 넉백과 같은 효과들을 구현합니다.
- 전투가 이뤄질 때 카메라의 연출, 화면 이펙트 등 다양한 장치를 추가하여 타격감을 형성합니다.